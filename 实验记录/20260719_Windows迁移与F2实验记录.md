# Windows 迁移与 F2 实验记录（2026-07-19）

## 记录约束

- 项目：TrackVLA Harness F2 / SA-Hstar。
- 当前平台：原生 Windows PowerShell，项目根目录 `E:\AAAI\track_car`。
- internal test 保持 sealed；本轮只读取交接许可的 cache manifest/provenance 路径与哈希元数据，`metadata_only=true`，未读取、展示或分析 internal-test 样本、标签、预测或指标。
- 不复用 Mac `assembly_receipt_v11.json`，不覆盖 `run_v1/run_v2/run_v3` 或其他历史产物。
- G6–G9 和 combined gate 全部通过前，不启动 formal 9-run。

## 环境基线（2026-07-19 23:45:11 +08:00）

| 项目 | 结果 |
| --- | --- |
| 训练 Python | `E:\anaconda\envs\pytorch\python.exe` |
| Python | `3.12.12` |
| PyTorch | `2.6.0+cu124` |
| CUDA runtime | `12.4` |
| `torch.cuda.is_available()` | `True` |
| GPU | `NVIDIA GeForce RTX 4060 Laptop GPU` |
| 显存 | `8188 MiB` |
| Compute capability | `(8, 9)` |
| NVIDIA driver | `560.94` |
| 默认 base 环境 | `torch 2.10.0+cpu`，禁止用于 CUDA 实验 |

已有 Conda 环境为 `base`、`fastpi`、`google_agent`、`pytorch`；遵守交接要求，没有新建环境。

## 冻结资产核对

| 资产 | Windows 路径/状态 | SHA-256 |
| --- | --- | --- |
| train JSONL | `data/collected_v1/datasets/train.jsonl` | `1715b3ce2c65df7caaa41d4a3f2f1eba61746e4b33158ae3267ad1477e96dd36` |
| cache manifest | `data/collected_v1/vision_cache/cache_manifest.json` | `127bda80a3d748f704b01bcf456c1e2e7c6c5b607f7eebe848fb5dc0e7824009` |
| cache provenance（manifest 内 canonical object） | metadata-only | `5399927be976e13f7c180143514f0863ac8687faf800fd1612cb3b9c42640ba4` |
| token payload literal | manifest 元数据；未遍历 sealed subtree | `f0016a2a25f8724ec45040eedb4ce73e54ca342ba1cf400a4c0a6ab0e1592744` |
| official base HF | `E:\AAAI\opentrackvla-qwen06b` | 预期 `ff1d31982271cb922c91a26f7767438124e12502e9341251041d8541f7d63a8f` |
| Qwen3-0.6B cache | `E:\AAAI\models--Qwen--Qwen3-0.6B`；blob 完整，snapshot 为 macOS `XSym` 占位，待物化 | 预期 `2f62d9a42d8cf3cd43a69155c345e024d0d5bd1590a701540c0f75aeae71162b` |
| DINOv3 | `weights/modelscope/dinov3-vits16-pretrain-lvd1689m` | `627c7bb4f39f79e15d5e3fdf61557172d11befbe0b42c6f4513bf3907f5fc7a1` |
| SigLIP cache | `E:\AAAI\models--google--siglip-so400m-patch14-384`；blob 完整，snapshot 为 `XSym` 占位 | `e9549756bf15a3ff2064c8a32f1086e9391f374682ca16a05c30f91fcbb5a096` |

cache manifest 仍保留冻结的 Mac producer 绝对路径。Windows 迁移采用字节保真 relocation：manifest/provenance 字节与 SHA 不变，运行时只在确认 manifest SHA 和已知 producer root 均匹配后，将 producer root 显式映射到 `E:\AAAI\track_car`；Windows assembly receipt 将同时记录 `recorded_path_root`、`effective_path_root`、`path_relocated=true` 和 `metadata_only=true`。

## 基线测试与失败留痕

### 第一次全套 F2 基线

命令：

```powershell
E:\anaconda\python.exe -m pytest -q -p no:cacheprovider tests\f2
```

结果：`6 failed, 129 passed, 1 skipped, 44 errors`。主要工程失败：

1. pytest 默认临时目录 `C:\Users\LENOVO\AppData\Local\Temp\pytest-of-LENOVO` 权限错误；后续测试改用任务专用 `E:\AAAI\pytest_tmp\...`，不将其解释为科学结果。
2. `str(Path)` 在 Windows 生成反斜杠，使 source receipt key 与固定 POSIX key 不一致，bootstrap 会 `KeyError`。
3. cache `path_root` 是 Mac 绝对路径，Windows fail closed。
4. `TokenHashLedger` 使用宿主平台 `Path.is_absolute()`，未拒绝 POSIX `/abs/...`。
5. official base 默认路径硬编码为 `/Users/...`。

### 第一批 Windows 兼容修复验证

修复内容：receipt 相对路径统一 POSIX；跨平台绝对路径/逃逸检查；冻结 cache 的显式 byte-preserving relocation；`F2_BASE_HF_DIR` 与同级 Windows asset resolver；macOS `XSym` 假快照拒绝；AppleDouble 文件加入忽略规则。

验证命令使用独立 basetemp：

```powershell
E:\anaconda\python.exe -m pytest -q -p no:cacheprovider `
  --basetemp E:\AAAI\pytest_tmp\batch1_targeted_03 `
  tests\f2\test_cli.py `
  tests\f2\test_assembly_data.py::test_frozen_constants_match_adjudicated_values `
  tests\f2\test_assembly_data.py::test_frozen_cache_roots_real_identity `
  tests\f2\test_assembly_data.py::test_frozen_cache_roots_rejects_foreign_path_root `
  tests\f2\test_assembly_data.py::test_token_ledger_validation_and_immutability `
  tests\f2\test_assembly_data.py::test_load_cached_observation_rejects_escaping_paths
```

结果：`14 passed in 6.06s`。

独立审查：已启动新 agent `review_windows_batch1`，结论待回填。

## Qwen `XSym` 权重物化

原始 cache 未修改：`E:\AAAI\models--Qwen--Qwen3-0.6B`。使用新工具 `scripts/materialize_hf_xsym_cache.py` 将 snapshot 中 6 个 `XSym` 占位解析到同一 cache 的 `blobs`，输出到新目录 `E:\AAAI\resolved_models\Qwen3-0.6B`。所有文件均成功建立 NTFS hardlink，并逐文件复算 SHA。

命令：

```powershell
E:\anaconda\python.exe -m scripts.materialize_hf_xsym_cache `
  --cache-root E:\AAAI\models--Qwen--Qwen3-0.6B `
  --output-dir E:\AAAI\resolved_models\Qwen3-0.6B `
  --receipt experiments\windows_cuda_f2\asset_materialization\qwen_xsym_materialization_v1.json `
  --mode auto
```

结果：

| 项目 | 值 |
| --- | --- |
| snapshot | `c1899de289a04d12100db370d81485cdf75e47ca` |
| 文件数 | 6 |
| materialization | 6/6 `hardlink` |
| Qwen artifact SHA | `2f62d9a42d8cf3cd43a69155c345e024d0d5bd1590a701540c0f75aeae71162b`（与冻结值一致） |
| materialization receipt SHA | `c286999818b95b6d212a8f6313ca0d03dbc4774bbf29407fd9d4c21ad95bbb0d` |
| Transformers 离线加载 | `Qwen3Config / qwen3 / hidden_size=1024`；`Qwen2TokenizerFast / vocab_size=151643` |

物化工具安全测试：`3 passed in 0.10s`。工具拒绝已有输出目录、拒绝 cache 外目标，并且不会覆盖原始 snapshot/blob。

随后运行 `verify_frozen_assets('.', verify_token_payload=False)`，得到：base artifact `ff1d3198...63a8f`、Qwen `2f62d9a4...1162b`、cache manifest `127bda80...824009`、cache provenance `5399927b...40ba4` 全部与交接冻结值一致；`path_relocated=true`、`metadata_only=true`、`internal_test_opened=false`。

## Windows 契约测试与审查闭环

- 既有 `pytorch` 环境只补装测试工具：`pytest 8.4.2`、`ruff 0.15.22`；未新建环境、未升级 torch/torchvision。
- F2 完整套件（CUDA 版环境、独立 basetemp）：`184 passed in 225.16s`。其后新增 1 个原生 Windows CUDA fail-closed guard 测试，针对性 `test_cli.py` 为 `10 passed`；最终 receipt 前会按当前源树再跑完整 185-test 套件。
- 真实 frozen-assets plan 构造测试单独结果：`1 passed in 173.59s`，覆盖 36,946-file ledger、official base/Qwen 加载、两臂组装与一次真实 EVAL forward。
- Ruff：`All checks passed!`。

独立 `review_windows_batch1` 发现并推动修复：manifest relocation junction 越界、token ledger/load junction 越界、显式权重路径被环境变量覆盖、Windows 测试符号链接权限。修复后其结论为无未解决 High/Medium。

独立 Qwen 物化审查与加固复核均通过：artifact 仍为 `2f62d9a4...1162b`；加固后 receipt 不得写在 output 内，只对不超过 64 KiB 的 `XSym` placeholder 解码，并覆盖真实 `/blobs/` 越界回归，`4 passed`、Ruff 全绿。

## Windows bootstrap receipt

`assembly_receipt_cuda_bootstrap_v1.json` 曾成功生成，但其后加入原生 Windows CUDA fail-closed guard，造成 source/test binding 漂移；v1 仅作为 predecessor 留存，不用于 CAL。

当前 authoritative bootstrap：

| 项目 | 值 |
| --- | --- |
| 文件 | `experiments/windows_cuda_f2/assembly_receipt_cuda_bootstrap_v2.json` |
| receipt SHA | `eb661120b5f509aa1365728ec89443adbaed5645647f313294ded551ea437fbd` |
| payload SHA | `686b2c9dc47deb5f1fa5e5a22a3d2acf3830939f0c83fabeb5b2564cb069c23a` |
| lambda binding | `null` |
| train ledger | `36946` files；`a963170846db0ef2890b45f056ec9884a713ce4223bea260722ccc25c62448d6` |
| base/Qwen/cache | 全部与冻结 SHA 一致 |
| internal test | `internal_test_opened=false` |

`verify_assembly_receipt` 已通过。

## CUDA seeded CAL 主跑

2026-07-20 00:31:25 +08:00 detached 启动：

| 项目 | 值 |
| --- | --- |
| 输出目录 | `experiments/windows_cuda_f2/cal_audit_cuda_v1` |
| Python PID | `31696` |
| GPU monitor PID | `32756` |
| bootstrap receipt | `assembly_receipt_cuda_bootstrap_v2.json` / `eb661120...437fbd` |
| stdout/stderr | `experiments/windows_cuda_f2/runtime/cal_audit_cuda_v1.*.log` |
| GPU telemetry | `experiments/windows_cuda_f2/runtime/cal_audit_cuda_v1.gpu.csv` |
| launch receipt | `experiments/windows_cuda_f2/runtime/cal_audit_cuda_v1.launch.json` |

启动后首次采样：GPU utilization `13%`，显存 `1586/8188 MiB`，温度 `57°C`；进程存活，stdout/stderr 暂无错误。未将该状态当作 CAL 结果。

### CAL v1 engineering burn

独立审查发现 bootstrap v2 的 `tests_sha256` 错误绑定了 10 个 macOS `._test_*.py` AppleDouble 侧车（21 项，而真实测试为 11 项）。这些侧车不参与 pytest，却会令未来清理造成 receipt 漂移。于 2026-07-20 约 00:35 +08:00 主动停止 CAL v1 PID `31696` 及遥测 PID `32756`；停止时尚无 `cal_audit_receipt_v1.json`。保留：

- `experiments/windows_cuda_f2/cal_audit_cuda_v1/`
- `experiments/windows_cuda_f2/runtime/cal_audit_cuda_v1.*`
- `cal_audit_cuda_v1.engineering_burn.json`

该目录明确标记为 engineering burn，不得当作 CAL 结果。

### 稳定树、bootstrap v3 与 authoritative CAL v2

修复 `_test_bindings` 后：

- 当前完整 F2：`185 passed in 226.56s`。
- Ruff：`All checks passed!`。
- bootstrap v3：`experiments/windows_cuda_f2/assembly_receipt_cuda_bootstrap_v3.json`。
- v3 receipt SHA：`84f9edc6961d2085893ac53923fb5311d1a8b5695231200268e2b2019e644f1d`。
- v3 payload SHA：`767d3dce7f9a62c4c5782be72acc86abd7720633992267d6d728e7abab31ed71`。
- binding counts：11 source / 15 transitive / 11 tests / 0 AppleDouble。
- `token_payload_verified=false`（遵守 sealed-test 禁止全 cache 遍历）；`metadata_only=true`；receipt 与 asset 两处 `internal_test_opened=false`。

2026-07-20 00:45:39 +08:00 detached 重启 authoritative CAL：

| 项目 | 值 |
| --- | --- |
| 输出目录 | `experiments/windows_cuda_f2/cal_audit_cuda_v2` |
| Python PID | `23316` |
| GPU monitor PID | `39276` |
| bootstrap receipt | v3 / `84f9edc6...44f1d` |
| runtime logs | `experiments/windows_cuda_f2/runtime/cal_audit_cuda_v2.*` |

该轮完成并生成合法 receipt 前仍不报告 CAL 数字。

## 后续固定顺序

1. 物化 Qwen（以及需要时的 SigLIP）`XSym` snapshot，逐文件校验并保存物化 receipt。
2. 在既有 `pytorch` CUDA 环境补齐测试工具，运行 F2 全套 pytest/Ruff。
3. 构建 Windows bootstrap assembly receipt（无 lambda binding）。
4. CUDA seeded CAL 主跑与复现。
5. 新建 CUDA lambda-freeze receipt，更新 frozen literals/pin test并重跑测试。
6. 构建 final Windows assembly receipt并验证 authority chain。
7. detached 启动 128-update paired smoke，保存 stdout、stderr、PID、GPU/显存、时长、checkpoints、EVAL、G6–G9 与 combined receipt。

## 状态续记：CAL v2 主跑完成（2026-07-20 00:50:53 +08:00）

本节 supersede 前文“CAL v2 尚未报告结果”的启动态描述；历史启动记录保留不改。主跑正常退出并以 exclusive-write 生成：

- receipt：`experiments/windows_cuda_f2/cal_audit_cuda_v2/cal_audit_receipt_v1.json`
- receipt SHA：`66fd328df2b2309683726f910f840642a6e7accdee94848406375775e4c5fac1`，与 stdout 报告一致。
- `rows=512`，`optimizer_updates=0`，`seed=0`，`device=cuda:0`。
- checkpoint init SHA：`74f838e314dd6f3b208dbed23e0e7a92dcdde09bb7d79b0ce8c1147d9b251e54`。
- step-0 parity、prev-free graph、AP2 zero-init violations 均为 `0`；`track_grad_norm_max=0.0`。
- bootstrap v3 file/payload SHA 分别为 `84f9edc6961d2085893ac53923fb5311d1a8b5695231200268e2b2019e644f1d` / `767d3dce7f9a62c4c5782be72acc86abd7720633992267d6d728e7abab31ed71`。
- token ledger：`36946` files / `a963170846db0ef2890b45f056ec9884a713ce4223bea260722ccc25c62448d6`。
- gradient medians：`L_cot=13.11864572209217`，`L_future=0.7549967769146092`，`L_verify=0.5137075979730261`。
- proposal：`L_cot=0.0196`，`L_future=0.34`，`L_verify=0.5`。
- `internal_test=sealed`，`internal_test_opened=false`；未读取或分析 sealed internal-test 内容。

GPU CSV 在 monitor 退出前因 Windows 重定向缓冲曾实时显示为 0 字节，停止 monitor 后正常 flush 为 100 个约 5 秒间隔样本。仅统计启动至 receipt 落盘的 63 个全卡样本：峰值显存 `4054 MiB`、利用率 `65%`、温度 `67°C`、功耗 `31.8 W`。这是整卡 telemetry，启动基线约 `1585 MiB`，不作严格 PID 级显存归因。stderr 只有 TensorFlow oneDNN INFO，无 traceback。

独立 agent `audit_cal_v2` 与 `audit_runtime_evidence` 均给出 receipt-level PASS；共同指出无显式 exit-code/completion sidecar 是非阻断工程缺口，后续长实验 wrapper 应补齐。

## CAL v2 独立复现与 exact-repro 阻断（2026-07-20 00:55:33–01:00:54 +08:00）

使用同一 Python、同一命令、同一 bootstrap v3 和全新目录 detached 运行：

- 输出：`experiments/windows_cuda_f2/cal_audit_cuda_v2_repro`
- Python PID：`39064`；GPU monitor PID：`4016`。
- receipt SHA：`cedc00b545554a8699e81931c7b71967b1b43d6801528d72de91fcbc2821e93c`。
- 运行期全卡峰值：显存 `4030 MiB`、利用率 `64%`、温度 `67°C`、功耗 `32.4 W`。

两份 receipt 大小均为 3843 bytes，但不 byte-identical。递归 leaf diff 恰有两项：

| 字段 | 主跑 | 复现 |
| --- | ---: | ---: |
| `gradient_calibration.per_aux_grad_norm_median.L_verify` | `0.5137075979730261` | `0.5137669470414328` |
| `gradient_calibration.aux_grad_norm_median_min` | `0.5137075979730261` | `0.5137669470414328` |

绝对差约 `5.93490684067e-5`，相对差约 `1.15535e-4`（`0.01155%`）。其余字段全部一致，包括 init SHA、seed/device、bootstrap/ledger、三类零违规；三项 3-significant-digit proposal 也完全一致（`0.0196 / 0.34 / 0.5`）。

独立协议审查确认 handoff 的最低要求允许“差异有明确解释”并继续 freeze，且这不是 G6–G9 科学性负结果，无需 F3。团队仍采用更严格的 exact-reproduction 标准：当前 v2 main/repro 原样保留为 `engineering_reproducibility_variance`，不从 bootstrap v3 签发 lambda freeze，也不启动 smoke。留痕：`experiments/windows_cuda_f2/runtime/cal_audit_cuda_v2.engineering_reproducibility_variance.json`。

只读诊断显示 seed/init 未漂移；缺口是原生 CUDA 路径没有冻结 `torch.use_deterministic_algorithms`、`CUBLAS_WORKSPACE_CONFIG`、cuDNN policy 与单一 SDPA backend。下一批工程修复保持 CAL support、batch、dtype、seed、lambda 公式和 gate 门槛不变，仅增加 fail-closed CUDA reproducibility contract；随后重新测试、独立 agent 审查、构建 bootstrap v4，并在全新目录运行两次 CAL。旧 v2 不覆盖、不删除、不称为科学负结果。

## 当前剩余顺序（supersede 旧清单）

1. 加入并测试原生 Windows CUDA deterministic runtime contract，同时把实际设置写入 CAL context receipt。
2. 完整 pytest/Ruff 后启动新的独立 agent 审查该代码批次。
3. 构建并验证无 lambda 的 bootstrap v4。
4. 新目录双跑 CUDA CAL；只有 receipt byte-identical 才创建 CUDA lambda-freeze receipt。
5. 更新 `FROZEN_AUX_COEFFICIENTS` 与 pin test，再次完整测试/独立审查并构建 final Windows receipt。
6. detached 运行 128-update paired smoke；保存 exit code、完成时间、PID、GPU telemetry、checkpoints、EVAL、G6–G9 与 combined receipt。
7. G6–G9 全 PASS 才允许 formal 9-run；科学性 FAIL 则封存 F2 并另立 F3。

状态更正：前文 Qwen “待物化”已由 `qwen_xsym_materialization_v1.json` supersede；当前 authoritative Qwen 目录是 `E:\AAAI\resolved_models\Qwen3-0.6B`，artifact SHA 为 `2f62d9a42d8cf3cd43a69155c345e024d0d5bd1590a701540c0f75aeae71162b`。

## 代码批次 A：Windows CUDA deterministic runtime contract（2026-07-20 01:14–01:40 +08:00）

新增 `f2_experiment/reproducibility.py`，并将其加入 assembly source binding；source 数由 11 增至 12，transitive 仍为 15，测试文件仍为 11。冻结合同：

- `CUBLAS_WORKSPACE_CONFIG=:4096:8`，必须在 torch-heavy assembly import/CUDA 初始化前准备；冲突值 fail closed。
- `torch.use_deterministic_algorithms(True, warn_only=False)`。
- `cudnn.deterministic=true`、`cudnn.benchmark=false`。
- matmul/cuDNN TF32 均关闭，float32 matmul precision 为 `highest`。
- SDPA 只允许 math backend；Flash、memory-efficient、cuDNN SDPA 全部关闭。
- 首次配置若检测 `torch.cuda.is_initialized()=true`，直接拒绝“事后补签”；只有同一 torch 模块已由本合同成功配置后，才允许幂等复验。
- CAL `cal_context` 必须完整记录 contract、Torch/CUDA 版本与所有实际 backend flags；CUDA context 缺项或字段漂移均拒绝。
- smoke 在任何 update/EVAL 前，将 plan 的 seed、规范化 device（`cuda:0`）、checkpoint init SHA 和完整 CUDA receipt 与 authoritative CAL context 逐项相等校验；smoke summary 同步落盘 runtime receipt。

没有改变 CAL/SMK/EVAL support、seed、batch、dtype、AMP、lambda 公式或 G6–G9 门槛。

验证：

1. 两个独立 Python/CUDA 进程执行 math-SDPA + `roi_pool_candidate` 同型 advanced-indexing backward，loss 均为 `1.7751833200454712`，`q_grad` SHA 均为 `d86cb7a4bddf21aa9238f075b1c1d314c806313ce2175cb768540cb7b7d9970e`，`grid_grad` SHA 均为 `8ca7c42a7c9de4b383dc97e7fadee87a96f244390a759af3e0ae11dddeae0b05`。
2. 独立进程先执行 CUDA matmul 再配置，正确抛出：`CUDA was initialized before the frozen Windows F2 reproducibility contract; deterministic policy is too late`。
3. 真实 frozen-assets smoke-plan 构造：`1 passed in 180.22s`，覆盖冻结 base/Qwen、36,946-file ledger、双臂构造与真实 CUDA EVAL forward。
4. 最终当前树：`191 passed in 218.18s`；Ruff `All checks passed!`。

按团队要求，代码批次完成后启动新 agent `review_cuda_repro_batch_a` 独立审查。初审发现“CUDA 已初始化后仍可补签”和“smoke plan metadata 未与 CAL 交叉绑定”两项 Medium，均已修复；复核结论 High/Medium 为 0，可进入 bootstrap v4。Low 建议是在 smoke 结束时再次读取 runtime policy 检测进程内全局 backend 漂移；当前源码未发现修改这些状态的路径，不阻断 v4。

## Windows bootstrap v4（2026-07-20 01:41 +08:00）

在 `191 passed`、Ruff 全绿和独立审查通过后，以 exclusive-write 新建：

- 文件：`experiments/windows_cuda_f2/assembly_receipt_cuda_bootstrap_v4.json`
- file SHA：`5aa6cc8ba13755bf65fe11e2f4573842c53309226f0b9226bba5de5bb9cc86a7`
- payload SHA：`8745585d9237744e5071f99337689d3f24759ee49f62344249d5abac1c881ea9`
- bindings：12 source / 15 transitive / 11 tests / 0 AppleDouble。
- `f2_experiment/reproducibility.py` 已进入 source binding。
- `lambda_freeze_binding=null`。
- ledger：36,946 / `a963170846db0ef2890b45f056ec9884a713ce4223bea260722ccc25c62448d6`。
- base：`E:\AAAI\opentrackvla-qwen06b`；Qwen：`E:\AAAI\resolved_models\Qwen3-0.6B`。
- cache：`metadata_only=true`、`token_payload_verified=false`。
- receipt 与 asset 两处 `internal_test_opened=false`，internal test 仍 sealed。

`verify_assembly_receipt` 已对当前工作树与冻结资产重新核验通过。v1/v2/v3 继续保留为 predecessor；后续新 CAL 只允许使用 v4。

## CUDA CAL v3 主跑与严格复现（2026-07-20 01:43–02:00 +08:00）

在 bootstrap v4 对应的 source/transitive/test 全部冻结不动时，依次运行预先指定的 main 与 repro；未并发其他训练任务，未启动第三次择优运行。

| 项目 | main | repro |
| --- | --- | --- |
| 输出目录 | `experiments/windows_cuda_f2/cal_audit_cuda_v3` | `experiments/windows_cuda_f2/cal_audit_cuda_v3_repro` |
| Python PID | `13492` | `33308` |
| GPU monitor PID | `37768` | `38472` |
| 启动时间 | `2026-07-20T01:43:45.4357631+08:00` | `2026-07-20T01:54:00.1341377+08:00` |
| receipt 落盘 | `2026-07-20T01:49:46.9479626+08:00` | `2026-07-20T02:00:01.5420926+08:00` |
| receipt SHA-256 | `e2b87cc3a088d68813349a3f33cb77db94eb43d2ff6c97659e45541479fee778` | `e2b87cc3a088d68813349a3f33cb77db94eb43d2ff6c97659e45541479fee778` |
| receipt bytes | `4347` | `4347` |

`Get-FileHash`、文件长度及 `ReadAllBytes` 三重比较均确认两份 receipt 严格 byte-identical，无 leaf diff。两次独立 receipt 均满足：

- `rows=512`、`optimizer_updates=0`、`seed=0`、`device=cuda:0`、`support=CAL`。
- step-0 parity `512/0`、prev-free `512/0`、AP2 `512/0`，`track_grad_norm_max=0.0`。
- bootstrap v4 file/payload SHA 为 `5aa6cc8ba13755bf65fe11e2f4573842c53309226f0b9226bba5de5bb9cc86a7` / `8745585d9237744e5071f99337689d3f24759ee49f62344249d5abac1c881ea9`。
- ledger 为 `36946` / `a963170846db0ef2890b45f056ec9884a713ce4223bea260722ccc25c62448d6`。
- checkpoint init SHA 为 `74f838e314dd6f3b208dbed23e0e7a92dcdde09bb7d79b0ce8c1147d9b251e54`。
- CUDA deterministic receipt 的 16 个字段完整一致：`windows_cuda_deterministic_v1`、cuBLAS `:4096:8`、Torch `2.6.0+cu124` / CUDA `12.4`、strict deterministic、cuDNN benchmark/TF32 关闭、precision `highest`、SDPA math-only。
- gradient medians 为 `L_cot=13.100267518450678`、`L_future=0.7527186011353795`、`L_verify=0.5114651815582008`。
- 新 proposal 为 `L_cot=0.0195`、`L_future=0.34`、`L_verify=0.5`；不得沿用 v2 的 `L_cot=0.0196`。
- `internal_test=sealed`、`internal_test_opened=false`；未读取或分析 sealed internal-test 内容。

main 的 receipt 有效窗口为 `361.512s` / 73 个 GPU 样本，GPU 利用率均值 `43.12%`、峰值显存 `3972 MiB`、最高 `67°C` / `34.2W`。repro 有效窗口为 `361.408s` / 73 个样本，利用率均值 `44.45%`、峰值显存 `3972 MiB`、最高 `68°C` / `35.93W`。两次 monitor 均在 PID 文件、launch receipt、进程名和完整命令核对后仅停止对应 `nvidia-smi` 进程，并等待 CSV flush；receipt 后 flush 样本未计入有效窗口。两次 stderr 均只有 TensorFlow oneDNN INFO，无 traceback。

严格 exact-reproduction 门槛已通过，可以进入新的 Windows CUDA lambda freeze；v2 variance 继续原样保留为 predecessor，不覆盖、不删除。

## 代码批次 B：lambda-freeze authority fail-closed 加固（2026-07-20 02:01–02:07 +08:00）

在 v3 main/repro 完成后才解冻源码。`f2_experiment/assembly.py` 的 freeze binding 与 runtime authority verifier 现会机械验证：

- freeze 顶层 schema 与 `internal_test_opened=false`。
- main CAL evidence 的实际 path/SHA、zero-update CAL 合同及 sealed 状态。
- freeze 复制的 `seed`、`device`、`checkpoint_init_sha256` 与 main CAL context 逐项一致。
- reproduction 使用不同实际路径，重新计算 SHA，并对 main/repro 执行真实 `read_bytes()` 逐字节比较。
- bootstrap receipt 实际 SHA、analysis/schema/receipt version、architecture、sealed 状态及 canonical payload self-hash；同时与 main CAL 记录的 bootstrap file/payload SHA 相等。
- authority 返回值保留 reproduction 与 bootstrap 的已验证 path/SHA/payload，供后续 smoke summary 留痕。

测试 fixture 已改用具备 canonical payload 的静态 bootstrap，并将 assembly roundtrip 从旧 unseeded freeze 更新为 authoritative seeded Mac freeze。新增拒绝分支覆盖：proposal 完全相同但 raw gradient median 漂移、复制的 seed/device/init 漂移、freeze seal 破坏、bootstrap bytes 漂移。

初次验证结果：定向 `7 passed`；完整 F2 `196 passed in 213.88s`；Ruff `All checks passed!`。独立新 agent `review_lambda_freeze_batch_b` 初审提出两个 Medium 候选：新 freeze 未强制显式 `internal_test=sealed`，以及 bootstrap payload evidence 对所有 receipt 都可省略。两项均已修复：只有 SHA 精确为 `4fe38c150e81382600dfc5ff62a228e6b60b9432ef301b7092186ee7d112f128` 的历史 seeded Mac freeze 才可兼容缺失旧字段；任何新 freeze 必须显式 sealed 且必须记录匹配的 bootstrap payload SHA。修复后定向 `7 passed`、完整 F2 `196 passed in 228.36s`、Ruff 全绿。

独立 reviewer 最终结论 High `0`、Medium `0`。Low `2`：单独 `verify_assembly_receipt()` 不会重验外部 CAL evidence，但生产 smoke 在 plan/update/EVAL 前必经完整 `verify_cal_lambda_authority()`；测试未直接命中 distinct-path 与 bootstrap canonical-self-hash 两个更深分支，但实现检查存在且审查确认正确。两项均不阻断 freeze/smoke，后续可在不改变实验合同的维护批次补测。source/transitive/test 文件数量仍为 `12 / 15 / 11`，但内容 SHA 已因本批代码变化而合法 supersede bootstrap v4；后续 final receipt 将绑定新树，CAL authority 仍通过 freeze 追溯 bootstrap v4。审查未访问 sealed internal test。

## Windows CUDA lambda freeze 与代码批次 C（2026-07-20 02:23–02:31 +08:00）

在 batch B 最新树 `196 passed`、Ruff 全绿且独立 reviewer High/Medium 为 0 后，以 exclusive-write 新建：

- freeze receipt：`experiments/windows_cuda_f2/cuda_lambda_freeze_receipt_v1.json`
- freeze SHA-256：`75a9823513ed96eadbb00d1b0a8d7b364900c1d3fbb5523522adc89e29ab9e14`
- frozen values：`L_cot=0.0195`、`L_future=0.34`、`L_verify=0.5`
- main/repro：分别绑定 `cal_audit_cuda_v3` 与 `cal_audit_cuda_v3_repro`，实际 SHA 均为 `e2b87cc3a088d68813349a3f33cb77db94eb43d2ff6c97659e45541479fee778`，各 `4347` bytes，`byte_identical=true`
- bootstrap v4：file/payload SHA 为 `5aa6cc8ba13755bf65fe11e2f4573842c53309226f0b9226bba5de5bb9cc86a7` / `8745585d9237744e5071f99337689d3f24759ee49f62344249d5abac1c881ea9`
- 显式记录 `internal_test=sealed`、`internal_test_opened=false`，并声明在任何 smoke optimizer update 或 EVAL-FIX 结果观察前冻结

新 verifier `_lambda_freeze_binding` 已对该 receipt 的 actual main/repro/bootstrap evidence 完整校验通过。随后才修改 `f2_experiment/assembly_model.py`：Windows assembly 的 `FROZEN_AUX_COEFFICIENTS` 从历史 MPS 的 `0.0197 / 0.341 / 0.5` supersede 为 `0.0195 / 0.34 / 0.5`；历史 MPS freeze/receipt 文件未修改。pin test 重命名为 `test_frozen_aux_coefficients_pin_windows_cuda_cal_v3_values` 并逐值锁定新 proposal。

代码批次 C 验证：定向 `2 passed`；完整 F2 `196 passed in 215.09s`；Ruff `All checks passed!`。已按要求请求另一个全新的独立 agent 审查 freeze/source/test 三方一致性，结论待回填。

独立 reviewer `audit_protocol_cal_diff`（与 batch B reviewer 不同）最终结论 High `0`、Medium `0`、Low `0`。其实际重读并比较了 main/repro bytes、freeze、bootstrap、source literal、pin test 和历史 MPS freeze SHA；确认时间顺序 bootstrap → CAL main → repro → freeze → literals/test，且审查时不存在 smoke 目录或 `run-smoke` 进程。独立 reviewer 单跑 pin test 时命中主机默认 `%TEMP%` 的既知 pytest 权限错误，属于 setup 环境问题；不改变使用任务专用 `--basetemp` 得到的完整 `196 passed` 证据。审查未访问 sealed internal test。

## Final Windows assembly receipt（2026-07-20 02:34 +08:00）

使用最新 `12 source / 15 transitive / 11 tests` 树和 Windows CUDA freeze receipt，以 exclusive-write 构建：

- 文件：`experiments/windows_cuda_f2/assembly_receipt_cuda_final_v1.json`
- file SHA：`330993715527df5a87888d9c477d73a82af213fec11cda290bec9412a2aecd19`
- payload SHA：`b40e3cf14448afd020a2ece55b1abc4e36619e05a27287b75982561baad22db4`
- lambda freeze binding：`75a9823513ed96eadbb00d1b0a8d7b364900c1d3fbb5523522adc89e29ab9e14`

`verify_assembly_receipt(..., asset_verifier=_default_asset_binding)` 与 `verify_cal_lambda_authority(...)` 均通过。authority chain 为 `via_lambda_freeze_supersede`：final receipt 绑定最新 source/test，freeze 向前追溯 bootstrap v4 与 byte-identical CAL v3 main/repro。机械断言确认 frozen lambda `0.0195 / 0.34 / 0.5`、seed `0`、device `cuda:0`、main/repro SHA 相同、bootstrap file/payload SHA 正确、`internal_test_opened=false`。现在才允许启动 128-update paired smoke。

## Detached Windows CUDA paired smoke 启动（2026-07-20 02:39 +08:00）

启动前确认 E: 剩余 `139.5 GiB`，GPU 基线约 `1581 MiB`，无其他 Python 训练任务。第一次 launch orchestration 预检因 PowerShell 字符串中 `${path}:` 变量分隔语法错误在 parser 阶段退出；该次未创建任何进程、输出目录或实验 artifact，不计作 smoke attempt。修正纯 orchestration 语法后，以 fresh exclusive targets 启动：

| 项目 | 值 |
| --- | --- |
| 输出目录 | `experiments/windows_cuda_f2/smoke_cuda_v1` |
| Python PID | `39428` |
| GPU monitor PID | `15464` |
| completion watcher PID | `36456` |
| 启动时间 | `2026-07-20T02:39:14.8959789+08:00` |
| final receipt | `330993715527df5a87888d9c477d73a82af213fec11cda290bec9412a2aecd19` |
| CAL main | `e2b87cc3a088d68813349a3f33cb77db94eb43d2ff6c97659e45541479fee778` |
| lambda freeze | `75a9823513ed96eadbb00d1b0a8d7b364900c1d3fbb5523522adc89e29ab9e14` |

环境显式冻结：`CUBLAS_WORKSPACE_CONFIG=:4096:8`、base `E:\AAAI\opentrackvla-qwen06b`、Qwen `E:\AAAI\resolved_models\Qwen3-0.6B`、HF/Transformers offline。GPU 以 5 秒间隔记录 timestamp/name/utilization/memory/temperature/power。独立 hidden PowerShell watcher 附着 Python PID，进程退出后用 exclusive-write 自动生成 `runtime/smoke_cuda_v1.completion.json`，记录 exit code、完成时间、stdout/stderr bytes 与 `smoke_summary.json` SHA；避免只凭 PID 消失推断成功。

启动 sidecars：`runtime/smoke_cuda_v1.{pid,gpu.pid,completion_watcher.pid,launch.json,stdout.log,stderr.log,gpu.csv,gpu.stderr.log}`。首次状态采样：三进程均存活，stdout/stderr 空，尚无 smoke artifact，GPU 约 `21% / 1590 MiB`；这只是加载/authority 阶段状态，不是实验结果。已将长跑只读监控交给独立 agent；若工程失败不覆盖目录，若完整科学 gate FAIL 不调门槛、不择 seed 重跑。

## F2 authoritative smoke 完成与科学负结果封存（2026-07-20 02:51 +08:00）

smoke 正常完成全部生命周期并退出；stdout 报告 `status=FAIL`、`formal_training_authorized=false`，stderr 仅两条 TensorFlow oneDNN INFO，无 traceback/OOM/contract error。运行从 `02:39:14.8959789` 至 completion `02:51:46.4218569`，共 `751.526s`。产物共 21 个文件，含四个 checkpoint+sidecar、update-0/update-128 EVAL、count receipt、gate inputs、G6–G9、combined 与 smoke summary；全部 actual SHA 已重算并与 summary/sidecar 一致。

关键完整性证据：

- `smoke_summary.json` SHA：`3c7144a2e8f07e558ddad654e2ab0abae9fab2e827729c0ba3b5de5ab7d40f99`
- `combined_smoke_gate_receipt.json` SHA：`689f3937e079db1e7a2d0c5fa8643c06dc14d9b0ca4359cc9c47805ba69ebe0f`
- `count_receipt.json` SHA：`90acf1e1d4c19c51e03ae154121caf57e39a1e48df1df09571b2ab51522f4937`；两臂各 128 optimizer steps / 256 rows，leak/overwrite/nonfinite reset 均 0
- update-0 checkpoint state SHA 两臂均为初始化 `74f838e3...251e54`；update-128 checkpoint/sidecar binding 全部通过
- final/CAL/freeze/bootstrap authority SHA 均与启动前一致；chain 仍为 `via_lambda_freeze_supersede`
- `valid_input=true`、internal test 始终 `sealed/opened=false`

Gate 结果：

| Gate | 结果 | 关键观测 | 冻结门槛 |
| --- | --- | --- | --- |
| G6 | **FAIL** | bstar aux/track median `6.442171`；cosine median `0.238405`；positive projection `80/120` | `<=1.5`；`>=0.6`；`>=108/120` |
| G7 S-CTRL | PASS | saturation `0`；method/base median `0.000724` | saturation `<5%`；total `<=1.0` |
| G7 S-SELF | PASS | saturation `0`；method/base median `0.000765` | 同上 |
| G8 | **FAIL** | S-SELF self-mode overall delta `+0.0125926`，四个 strata 全部恶化；gap contraction 与低于 CTRL 均失败；logged ceiling 通过 | improvement `<=-1e-6` |
| G9 S-CTRL | **FAIL** | range violation `649/2048 = 31.689%` | `<5%` |
| G9 S-SELF | **FAIL** | range violation `507/2048 = 24.756%` | `<5%` |

G6 的 reachability 通过，说明不是 auxiliary/track 断图，而是辅助梯度在训练期过强且方向不一致；fallback per-aux ratio `L_cot=3.02587`、`L_future=2.07903`、`L_verify=4.44944` 均失败。G9 的 nonfinite、reconstruction、static reset 与 drift-ratio 检查均通过，失败集中于 raw action range。Combined 为 `FAIL / STOP`，`formal_training_authorized=false`，summary 的唯一 next step 是 `seal_negative_result_no_retry_no_gate_tuning`。

GPU telemetry：CSV SHA `3bb1c8e8113c50878887d1fb6355a7541afe941e866424cf666f859d56dd9321`，160 行；进程有效窗口 150 样本 / `747.305s`，利用率 avg/min/max `47.15/22/63%`，显存 avg/min/max `5102.4/1587/6667 MiB`，最高 `69°C / 32.76W`。monitor PID/命令核对后仅停止对应进程，completion 后 10 行 flush 样本未计入有效窗口。

completion watcher 成功观察进程结束并绑定 summary SHA，但 Windows 上附着既有进程时 `ExitCode` 被序列化为 `null`，且无 watcher exception。原始 completion 保持不改；新增 `runtime/smoke_cuda_v1.exit_code_adjudication_v1.json`（SHA `17afd4d8a28957aa1ed2808cab6e3953223faf1913f6ea96360c9599aa23fee7`），明确区分“OS exit code 未观测”与 final-receipt 绑定 CLI 合同 `return 0 if passed else 2` 推导出的 code `2`，不得把推导值冒充 OS 观测。

负结果 seal：`experiments/windows_cuda_f2/f2_smoke_negative_result_seal_v1.json`，SHA `b85585c8232f65c75d5958abb7d51d7624db4031c9adec86ee570d0a5b7378e7`。该 receipt 明确 `valid_input=true`、`engineering_failure=false`、`scientific_negative_result=true`，禁止同一 F2 重跑、挑 seed、调 gate 或启动 formal 9-run。后续改进只能建立全新 preregistration/family，保留本次 F2 证据不变。

## F2 负结果 forensic 分桶与诊断缺口（2026-07-20）

独立 agent 对 authoritative `gate_inputs.json`、G6/G7/G8/G9 receipts 与冻结源码做了只读复核；未读取 sealed internal test，未重跑 F2。结论是本次 FAIL 不能归因于 Windows 迁移、断图、计数缺失、reset 异常或非有限值，而是两个可分离的模型机制问题。

G6 在 `u_pre=8..127` 的时间分桶如下。ratio 是 `||Σ λ_i g_aux_i|| / ||g_track||`，cosine 是 aggregate total 与 track 的方向一致性；“正投影”统计 signed projection `>0` 的 update 数。

| u_pre | ratio median | cosine median | 正投影 | 主要过强项 |
| --- | ---: | ---: | ---: | --- |
| 8–15 | 6.46 | 0.176 | 6/8 | 混合 |
| 16–31 | 1.84 | 0.445 | 15/16 | `L_future` |
| 32–47 | 2.46 | 0.555 | 13/16 | `L_cot` |
| 48–63 | 21.42 | 0.293 | 11/16 | `L_verify` |
| 64–79 | 4.72 | -0.283 | 6/16 | `L_verify` |
| 80–95 | 21.75 | 0.475 | 12/16 | `L_verify` |
| 96–111 | 9.90 | 0.005 | 8/16 | `L_verify` |
| 112–127 | 16.29 | 0.005 | 9/16 | `L_verify` |

局部证据：`u20..24` 与 `u34..39` 仅短暂出现低 ratio；`u48` 以后 80 个 update 中有 66 个由 `L_verify` 提供最大 weighted per-aux ratio；`u58` aggregate ratio 达 `1117`，`u117` 达 `299`；`u71..78` 连续八个 update 为负 signed projection；`u96..111` 没有任何 cosine `>=0.6`。与此同时 G7 的 method/base 与 previous-action routing 量级都约为 `1e-3` 且无 saturation，说明不是 routing scale 爆炸，而是辅助损失通过共享 `base.proj` 强烈改写表征、method stream 本身却近乎关闭。

G9 的直接机制是 AP2 使用无界 `prev + cumsum(delta)` 形成 raw horizon；controller clamp 只处理 k0 的发送路径，future clamp 只是 detached telemetry，不约束训练使用的 raw head。S-CTRL `649/2048`、S-SELF `507/2048` 的越界分母均精确等于 256 rows × 8 horizons。现有归档只能确定任一 controlled axis 越界的聚合数，不能定位到 forward/yaw、k0/k1..k7、update、row、正负边界或 overshoot 大小。

因此下一新 family 在任何 optimizer update 前必须冻结并保存以下新增诊断：

- G6：逐 update 的绝对 `||g_track||`、`||g_aux_i||`、`||Σ λ_i g_aux_i||`、`||g_total||`，各 aux 对 track 的 cosine/signed projection、near-zero denominator 标志、clip 前后范数以及 `base.proj`/head 参数更新范数。
- G9：arm × update × row-within-update × horizon × axis 的 boundary hit/violation，正负方向、overshoot 的 max/p50/p90/p99、k0 与 k1..k7 分离，以及同步的 previous action、逐步 residual、pre-bound action、最终 raw action与 prev source。
- 分桶固定为 warmup `8..15`，随后每 16 updates 一桶，同时保留逐 update 原始记录；不得只保存聚合 gate 输入。
- EVAL-FIX 保存逐轴/逐 horizon range 统计和 paired row-level update0→update128 delta；同时预注册 G6 与 G9 的同-update及固定 lag 联合统计。

历史文档已经把 `F3` 保留给 dormant 的 `H*-ABS`↔`H*-SA` representation package ablation，`docs/agent_team_plan.md` 也使用 F3 表示 PFEM 状态一致性。因此后续 bounded-residual/gradient 改进不得直接命名为 F3；须使用不冲突的新 namespace，并建立独立 PRIMARY preregistration、source/CAL/gate/claim receipts。F2 formal 9-run 在本轮永久取消，不再列为待运行事项。

## IBR1 PRIMARY 预注册冻结（2026-07-20 03:31 +08:00）

新 successor family 冻结为 `IBR1`（`interval_bounded_residual_v1`），architecture lock 为 `L1+D2+IBR1-NCBR+F2-SMOKE-CONTRACT`。它不复用历史 F3/F4，也不是 F2 retry。PRIMARY authority 透明记录为用户委托的本地 Codex agent team；没有冒充 Fable 新审查，`historical_fable_primary_superseded=false`、`external_model_review_completed=false`。

唯一行为改动是把 F2 的无界 decode

`a_raw = prev + cumsum(latent_delta)`

替换为无超参数的 normalized cumulative bounded residual：

`z = cumsum(latent_delta)`

`a_raw = (prev + z) / (1 + abs(z))`

由 `|prev+z| <= 1+|z|` 可知每轴构造落在 `[-1,1]`；zero-init 时 `z=0`，逐位精确 persistence。继承字段 `delta_fy` 定义为相邻 `a_raw` 的 realized increment，保证 `prev+cumsum(delta_fy)` 仍 telescoping 重构；新增 latent/prebound/normalizer/overshoot telemetry。首试禁止同时加入 auxiliary isolation、gradient guard、routing、lambda、support、seed、optimizer、exposure 或 gate 改动。

PRIMARY index：`experiments/windows_cuda_ibr1/preregistration/ibr1_primary_preregistration_v1.json`

- file SHA：`b08d1d001b2178d13abd30dc94f1a2d24c574965ad200b2dff1f87320c8b2007`
- payload SHA：`5de7cdf41f21f4db03d43d12ea6e0f5e8ea0c5d7e730bdac9df93cf5d7d3b9b8`
- 绑定 6 个 canonical component receipts：negative-result adoption、architecture、gradient policy、CAL protocol、gate registry、ablation/claim
- 绑定 PRIMARY prompt/command/raw/verdict 的整文件 SHA
- 7 个 JSON receipts 均验证 `canonical_json_bytes(document)+LF`、payload self-hash 与实际 file SHA
- F2 negative seal 实际 SHA 再次核对为 `b85585c8232f65c75d5958abb7d51d7624db4031c9adec86ee570d0a5b7378e7`

IBR1 仍使用 `IBR1-CTRL/IBR1-SELF`、seed 0、128 updates、16-update self-exposure warmup、冻结 lambda `0.0195/0.34/0.5`。fresh CAL 仅作新 identity/no-drift audit：main/repro 必须 byte-identical，proposal 必须逐值等于继承 lambda；差异即 STOP，不允许重新选系数。Smoke 的 post-decode range=0 只是结构门；projection 前 `additive_prebound_fy` 越界率仍须两臂严格 `<5%`，且原 G6/G7/G8 全过才算 mechanism PASS。即使全过，IBR1 也不直接授权 formal training；任何失败均封存且不换 seed、不调 gate、不改 decode 后同名重跑。internal test 继续 sealed。

## IBR1 代码批次 1：bounded action geometry（2026-07-20）

为保持 F2 final receipt 可复核，本批次没有修改 `f2_experiment/` 或 `tests/f2/`，新建独立 `ibr1_experiment` namespace：

- `ibr1_experiment/__init__.py` SHA `772aced1300317fe6b2e133a44e8ba31e7ec1015cc25a886a7efc708bc0c19af`
- `ibr1_experiment/model.py` SHA `89e81ef67c7d62221e4d8adea1bd758c30174891dd893e0f84ac432912ec4d9b`
- `tests/ibr1/test_ibr1_model.py` SHA `18da6407092dbc102073304ac880a3864cff7e7ea073ddd3bf11006f16a4b98b`

实现要点：

- `IBR1NormalizedBoundedHead` 保留 F2 trunk/branch 参数名、形状、初始化顺序与 RNG consumption；同 seed 下 F2/IBR1 state tensor 实际 bytes、keyset 与构造后 RNG state 相同。
- trainable branches 输出 `latent_delta_fy`；`cumulative_latent_fy=cumsum(latent_delta_fy)`；`raw_fy=(prev+cumulative)/(1+abs(cumulative))`。
- 继承的 `AP2Prediction.delta_fy` 保存 realized increment，使旧 runner 的 `prev+cumsum(delta_fy)` 在 FP32 压力测试中最大误差 `4.768e-7 < 1e-6`。
- `raw_actions` 是 model/controller 之间的构造有界动作；`bounded_future_actions` 保持 detached；新增 cumulative/prebound/normalizer/overshoot/boundary-margin telemetry。
- authoritative geometry 只允许同 device、同 dtype FP32；FP64 仅审计；FP16/BF16/mixed dtype/AMP fail-closed，不静默 cast。

独立 reviewer 在 CAL/训练前发现原 PRIMARY 的 `bit-exact` 会被误解为 `-0.0/+0.0` sign-bit identity。已新签 `ibr1-primary-amendment-1`：

- receipt SHA `e054023f3f86a1e7718a2f76717f31ef9b5b58a4633293725ce989c3e000942b`
- payload SHA `d0b2a581ec86a40eff0f0c58ed55a0783ef47a577f66ca82e0a8630db64d3cbc`
- zero-init persistence 收紧为 same dtype/device 上的 `torch.equal` 数值 exact，不声明 signed-zero bit identity
- decode、参数、lambda、seed、optimizer、support 与全部 gate threshold 均未改变

诊断 schema 也在任何 CAL/smoke 前冻结：

- schema SHA `bc4835044a974e7e0215407d2efed4aa692003545d1a920060dd4b2902afcacb`，payload `29907fbb76fa08a96f3c1c835077b3961d414c693384a374eeec3dcb16161a4b`
- schema amendment SHA `444a5668733eb27cf777b24a28bff9fd744034129af0857284007b78734dfaac`，payload `546a12c9b8db134da8a4305c9778d48f205037d1ed621666f1d95dc32ca44707`
- I2 只使用 SMK-TRAIN branch2；每臂 256×8=`2048` 个 any-axis horizon observations
- overshoot quantile 使用每臂 4096 个 axis cells（含 0）的 nearest-rank；violating-only quantile 仅作次级描述
- `u_pre=0..7` 保留独立 bucket 且计入 I2 overall；G6 contribution 只记录 CTRL，optimizer geometry 两臂各 128 条；EVAL paired geometry 是 mandatory descriptive，不增加新 gate

验证：

- 定向 IBR1：`12 passed`
- 完整 F2+IBR1：`208 passed in 218.14s`
- Ruff：`All checks passed!`
- reviewer 最终：High `0`、Medium `0`；仅保留非阻断 Low（公共非法 dtype 的异常基类命名、mixed-device/AMP 自动化覆盖、边界外向方向固有平坦），均不得被夸大成机制 claim
- F2 final receipt 绑定的 12 个 source SHA 与核心 tests SHA 再次逐项核对未变化
- internal test 未访问，仍为 sealed/opened=false

## IBR1 代码批次 2A：assembly 与非行为诊断（2026-07-20 15:11 +08:00）

本批次在任何 IBR1 CAL、EVAL 或 optimizer update 前完成。没有修改 `f2_experiment/`、`tests/f2/`、sealed F2 smoke 产物或任何 preregistration/gate threshold；也没有访问 internal test。实现仍只改变 IBR1 action decode，新增内容均为 assembly identity 或 detached diagnostics。

新增/更新文件与 SHA256：

- `ibr1_experiment/__init__.py`：`5f5244d3cbb66fedcaa29c62ae3fb9068a5f18e0f060db9a43ea53bf4eca69e1`
- `ibr1_experiment/assembly_model.py`：`49533e4db4ce8668c7970307fbc70af60b4cff1acf914b3c5ab2d7e7015632de`
- `ibr1_experiment/diagnostics.py`：`8bae28a3f201c4b4753cd2f75aa9cf6dd770fb49acaae06cab8bc4b8f2c04abe`
- `tests/ibr1/test_ibr1_assembly_model.py`：`491f7929d1186e442f403b9e95e183d982768327ca722e5d3e649cd38acdc41d`
- `tests/ibr1/test_ibr1_diagnostics.py`：`9ce8ca7d2885382c23d650042f72e9e1da214029099d8cc436e264b128005cc1`
- 批次 1 的 `ibr1_experiment/model.py` 与 `tests/ibr1/test_ibr1_model.py` 保持原 SHA 不变。

Assembly 实现：

- 独立构建 `base → OpenTrackVLAF2ObservationAdapter → IBR1AP2Model`，只开放 base 内的 `base.proj`，其余 adapter/fusion/head trainable policy 与 F2 完全继承。
- 先构建 engine `S-CTRL`，再 `deepcopy` 出 `S-SELF`；公开映射固定为 `IBR1-CTRL/IBR1-SELF`。
- 两臂 parameter object、optimizer object、optimizer parameter membership 均强制不共享；optimizer parameter coverage 必须精确覆盖每臂 trainable parameters。
- seed-0 init SHA 不在 IBR1 源码中手抄。`read_sealed_f2_init_evidence()` 动态验证 `IBR1 PRIMARY → negative-result adoption → F2 negative seal → authoritative smoke summary → 双臂 update-0 state SHA`，解析出 `74f838e314dd6f3b208dbed23e0e7a92dcdde09bb7d79b0ce8c1147d9b251e54`；任一 path/SHA/self-hash/seal/status/seed 漂移均 fail closed。
- IBR1 optimizer receipt 使用独立 analysis class 与 IBR1 architecture/family/arm identity，不把 IBR1 冒充成 F2 receipt。

Diagnostics 实现：

- `IBR1G6Instrument` 继承原 F2 `G6Instrument`；在父类清空 accumulator 前复制真实两行 accumulation，并按 live `event.grad_accum=2` 求平均。父类 `G6Update` 仍原样产生，绝对诊断不替换 inherited G6 gate geometry。
- 每个 CTRL update 保存 `g_track`、weighted aggregate aux、total、dot/cosine/signed projection、每个 aux 的 weighted norm、按冻结 lambda 反推的 raw norm及 near-zero finite sentinel。
- aggregate aux 与逐项 per-aux 的一致性使用 source-FP32 rounding-aware bound：逐元素 `16×eps_fp32×Σ|g_i| + tiny_fp32`。独立 reviewer 证明旧的固定 `rtol=1e-6, atol=1e-9` 在 100 万维随机三梯度中会误杀 4,981 个元素；修复后同一探针 violations=`0`，并有“合法 cancellation PASS / omitted component FAIL”对抗测试。
- optimizer diagnostics 的外层 wrapper 在原 callback 前读取 pre-clip grad 和参数快照；AdamW step pre-hook 读取原 `clip_grad_norm_` 后的真实 grad；post-hook 读取真实参数更新。原 callback 继续唯一执行 clip、`step()`、`zero_grad(set_to_none=True)` 与 adapter audit，没有复制数值路径。
- training geometry 仅包装 branch2，保留原 `HeadForwardResult` object；默认严格要求每臂 256 行、总计 512 行。I2 使用每臂 `256×8=2048` 个 any-axis horizon observations，严格 `<5%`，测试锁定 `102/2048 PASS`、`103/2048 FAIL`。
- overshoot 的 deciding universe 独立使用每臂 4096 个 axis cells（含 0），nearest-rank p50/p90/p99；violating-only support 为 0 时保留 null 语义。`u_pre=0..7` 明确保留且进入 overall。
- EVAL geometry 默认严格要求 3 snapshots × 2 modes × 512 rows × 8 horizons × 2 axes = `49,152` cells；SELF update0→128 必须精确配对 `16,384` keys。EVAL 只作 mandatory descriptive telemetry，不增加科学阈值。
- reviewer 负向探针证明仅凭总 cardinality 会允许 EVAL mode/position coverage 被替代；最终实现增加 live snapshot×mode position clock、final 每个 snapshot×mode 的 `0..511` 精确覆盖和一位置一 original-row 约束。
- I2 不再信任 prediction 携带的 mask：要求 bool `(1,8,2)`、同 device，并逐元素精确等于 `abs(additive_prebound_fy)>1`。同时补齐 `row_within_update`、arm×u_pre×row-within×horizon×axis×正负联合计数，以及按 axis/horizon/u_pre/registered time bin 的含零 nearest-rank overshoot quantiles。
- 缺行、重复、clock drift、shape/dtype/device drift、NaN/Inf、错误 arm/snapshot/branch 均在 gate adjudication 前工程 fail closed。

验证留痕：

- IBR1 定向：最终 `27 passed`（model 12 + assembly 5 + diagnostics 10）；最终单独复验 `27 passed in 11.15s`。
- optimizer diagnostics on/off 单步对照：最终 model tensors、AdamW state、grad clearing 与 RNG state 逐项一致。
- 第一次完整回归通过 `conda run` 启动后，外层命令在 604 秒达到工具上限且 stdout 未保留；子进程随后自然结束。该次只记为 test-infrastructure timeout，不作为通过或失败证据。
- 第二次直接使用 CUDA 环境 Python、但未指定 basetemp：`167 passed, 53 errors in 167.20s`；53 项全部是 pytest fixture setup 对系统临时目录 `C:\Users\LENOVO\AppData\Local\Temp\pytest-of-LENOVO` 的 `PermissionError [WinError 5]`，没有 test assertion failure。
- 使用全新工作区 basetemp `.pytest_tmp_ibr1_full4` 首次完整复验：`220 passed in 194.64s`。
- reviewer 加固后使用另一全新 basetemp `.pytest_tmp_ibr1_full5` 最终完整复验：`222 passed in 196.66s`。
- rounding-aware 最后修改后尝试 `.pytest_tmp_ibr1_full6` 全量回归，外层再次在 604 秒触发工具上限且未返回断言失败，子进程随后自然结束。最终验收采用可完整复核的组合证据：上一轮 F2+IBR1 `222 passed`（其中 F2 196 项）+ 最终 IBR1 `27 passed` + 全量 Ruff；最后修改只涉及 IBR1 diagnostics 与其测试，F2 source/transitive 27 项仍为 mismatch `0`。
- Ruff：`All checks passed!`，范围为 `f2_experiment ibr1_experiment tests/f2 tests/ibr1`。
- F2 final assembly receipt 绑定的 `source_sha256 + transitive_source_sha256` 共 27 个文件逐项重算，mismatch `0`。
- 当前既有环境 `E:\anaconda\envs\pytorch\python.exe` 为 PyTorch `2.6.0+cu124`，`torch.cuda.is_available()==true`，GPU `NVIDIA GeForce RTX 4060 Laptop GPU`；默认 base Conda 的 Python 是 CPU 版，后续 authority/CAL/smoke 禁止误用默认 Python。
- `F2_BASE_HF_DIR`/`QWEN_MODEL_PATH` 当前 shell 未设置，但 portability resolver 已解析 base 为 `E:\AAAI\opentrackvla-qwen06b`；2B 必须继续通过 receipt/asset verifier 解析并绑定，不得依赖静默 CPU 或在线下载。
- 真实 CUDA 零更新 assembly 探针完成：本地 base `E:\AAAI\opentrackvla-qwen06b`、Qwen `E:\AAAI\resolved_models\Qwen3-0.6B`，missing/unexpected keys 均为 `0`；`S-CTRL→deepcopy S-SELF` 构建耗时 `11.81s`，公开 arms 为 `IBR1-CTRL/IBR1-SELF`，实际 init SHA 与 sealed F2 均为 `74f838e314dd6f3b208dbed23e0e7a92dcdde09bb7d79b0ce8c1147d9b251e54`。未执行 backward、CAL、EVAL 或 optimizer step。

Agent-team 留痕：最初启动的 assembly、diagnostics 与 CUDA preflight 三个新 agent 均因服务端余额不足返回 `403 Forbidden`，没有产生代码写入；主线程接管实现与预检。批次完成后按用户要求启动全新 `review_ibr1_batch_2a` 本地 Codex 只读 reviewer（没有冒充外部模型）。reviewer 最终结论：High `0`、Medium `0`、Low `1`；审查期间发现并推动修复 EVAL coverage、mask trust 与 FP32 cancellation 三类问题。

reviewer 唯一 Low：`GeometryCollector` 本身尚未强制六个 EVAL snapshot/mode 共用同一套 `position→original_row_index` 映射。现有 F2 evaluator 会复用同一 `eval_rows`，故不阻断 2A；2B thin authority/orchestrator 必须把固定 EVAL-FIX 顺序绑定为硬校验，不能只依赖 collector 各 pass 内部完整。

本批次状态：代码、本地回归、真实 CUDA init 探针与独立 reviewer 均完成；IBR1 CAL/smoke 尚未启动，`formal_training_authorized=false`，internal test 继续 `sealed/opened=false`。

## IBR1 代码批次 2B 阶段性审查与返工留痕（2026-07-20 17:32 +08:00）

本节记录的是代码与 authority 审查状态，不是实验结果。截止本节写入时，没有运行真实 IBR1 CAL、EVAL、backward、optimizer update 或 authoritative smoke，也没有访问 internal test。

已完成 checkpoint 加固：`ibr1_experiment/checkpoint.py` 与 `tests/ibr1/test_ibr1_checkpoint.py` 的独立 reviewer 结果为 High `0`、Medium `0`、Low `1`；定向测试 `17 passed`，Ruff 通过。唯一 Low 是 evaluation snapshot 暴露的 final assembly receipt 为可变字典。主线程已将其改为 `MappingProxyType` 并补充不可变性测试，修复后定向复验仍为 `17 passed in 4.38s`，Ruff 通过；该小修等待最终窄复核。

Windows 启动与留痕补充：

- `ibr1_experiment/__init__.py` 已改为 PEP 562 lazy exports，隔离子进程证明仅 `import ibr1_experiment` 不加载 `torch` 或 `ibr1_experiment.model`；测试 `2 passed`，Ruff 通过。此项用于保证 native Windows CLI 能在首次导入 PyTorch 前设置冻结的 cuBLAS/CUDA 确定性环境。
- 新增 `ibr1_experiment/artifacts.py` 与对应测试，锁定 diagnostics 原始产物 cardinality：training `512`、EVAL `49,152`、gradient `128`、optimizer `256`；JSONL 使用逐行 sorted compact finite JSON + LF，summary/manifest 使用 canonical JSON + LF 和 payload self-hash，manifest 最后写入并绑定 SHA/bytes/records。定向测试 `5 passed in 3.97s`，Ruff 通过；等待独立 reviewer。
- 新增真实模型侧 `ibr1_experiment/calibration_model.py`：复用 frozen F2 `CalRowAuditor`，对真实 `RunnerRow`/adapter/IBR1 forward 测量 FP32 zero-init persistence、post-decode range、`(8,2)`/16 cells、telescoping reconstruction 与 prev-free graph；默认 production factory 强制 `cuda:0` 且禁止 CPU fallback。定向 `8 passed`、当时全 IBR1 `119 passed`、Ruff/py_compile 通过；等待独立 reviewer。

authority/calibration 首轮独立审查没有验收，必须保留为 BLOCK 证据：

- `authority.py` reviewer：High `1`、Medium `1`、Low `0`。High 是 execution witness 只验证自述 PID/token、callback count 与 artifact SHA，未绑定 512 条实际 callback transcript；手工构造两份合法自哈希 witness 仍可被 `verify_cal_pair()` 接受。Medium 是 `verify_cal_envelope()` 未显式要求 envelope/core 绑定同一 bootstrap。
- `calibration.py` reviewer：High `1`、Medium `1`、Low `0`。High 是公开入口接受任意 subordinate runner/auditor，现有 fake harness 可以在不运行真实模型/CUDA CAL 的情况下生成可推进 authority 的证据。Medium 是一次性 latch 仅存 module globals，删除 `sys.modules` 后重导入可重新开放同一 OS 进程的 CAL 机会。
- 两次 reviewer 的现有定向测试均通过，只说明旧测试没有覆盖上述 authority 缺口，不能推翻 BLOCK 结论。

已启动返工且尚未验收：公开 CAL 入口将固定 frozen F2 runner 与真实 `calibration_model` auditor；fake seam 只能生成 `authority_eligible=false` 的 test-only 产物；一次性状态迁移到随解释器稳定的 process registry；witness/numeric 增加 512 条 callback transcript 与链式 SHA，authority 将逐条重算 frozen row order、F2 medians、IBR1 shape/cells/aggregates，并补 envelope→core→bootstrap 闭环。返工完成并经新的独立 reviewer 清零阻断项之前，禁止生成真实 bootstrap/CAL authority，更禁止启动 smoke。

冻结边界保持不变：F2 authoritative smoke 继续为 `FAIL / STOP`，negative seal SHA 继续为 `b85585c8232f65c75d5958abb7d51d7624db4031c9adec86ee570d0a5b7378e7`；不得重跑 F2、换 seed、调 gate 或启动 formal 9-run。IBR1 即使未来全部 mechanism gates PASS，`formal_training_authorized` 仍必须为 `false`；internal test 继续 `sealed/opened=false`。

## 2B 首轮 reviewer 追加阻断（2026-07-20 18:28 +08:00）

本节仍不是实验结果；真实 IBR1 CAL/EVAL/smoke/backward/optimizer 均未运行。

- 纯组装层 `smoke_model.py` reviewer：High `1`、Medium `1`、Low `2`。低层 factory 接受两臂共享同一 ArmAssembly/modules/optimizer，并会给同一 optimizer 重复挂 hooks；同时未机械复核 live AdamW groups/parameter receipt/device。返工已启动，要求拒绝所有跨臂 object/parameter/optimizer 共享，并补 reset-set identity SHA。
- I1–I6 `gates.py` reviewer：High `3`、Medium `3`。发现真实 final binding 参数路径不可用且省略时可绕过，I6 reconstruction 可用 `128×2` 嵌套值伪装成 256 行，seal 未追踪 diagnostics manifest 内部 artifact bytes/SHA，另有负指标、语义授权别名和任意 synthetic gate mapping 可通过。返工已启动，要求 live artifact validator、严格 row cardinality、manifest逐文件重哈希、递归语义拒绝以及固定 I1–I6 schema。
- CAL/authority 第二轮 reviewer：仍 High `1`、Medium `1`。High 是 file-only pair 仍可手写并推进 freeze；Medium 是 transcript 对 `sequence/frame/mirror/prev/reasons` 绑定不完整。返工方向已升级为 forensic file pair 不再具备 authority eligibility，正式 freeze 只能由固定父进程实际启动官方 main/reproduction worker、持有 live proof 后在同一调用内完成；bootstrap 将封存完整 512 行 CAL execution binding。

以上阻断项全部清零并经新的独立 reviewer 复核前，严禁生成 bootstrap/CAL authority、lambda adoption freeze、final assembly 或 authoritative smoke。所有 reviewer 均确认没有访问 internal test，且 F2 negative seal 不变。

## 2B smoke_model 第二轮独立复审 BLOCK 与 Windows CLI 批次（2026-07-20 18:48 +08:00）

本节只记录代码与审查状态，不是实验结果。真实 IBR1 CAL、EVAL、backward、optimizer update、authoritative smoke 均未运行，internal test 仍为 `sealed/opened=false`。

`smoke_model.py` 返工后的全新只读 reviewer 结论为 BLOCK：High `2`、Medium `1`、Low `0`。旧的 `14 passed`、相关 `36 passed` 与 Ruff 全绿不能覆盖以下反例：

- High：两臂只拒绝 Parameter object ID 共享，没有拒绝底层 tensor storage 区间共享。把 SELF 的 `Parameter.data` 指向 CTRL 的同一 storage 后，object IDs、init SHA 与机械 AdamW receipt 仍可通过，任一臂 step 会串改另一臂。
- High：公共 component factory 可由调用者提供 `_production_context`、receipt/binding 与任意 `IBR1SmokeData`，在 final receipt 文件不存在、reset set 被伪造时仍生成 `ibr1_production_smoke_assembly_plan`。production authority 必须改为仅由 live receipt/data loader 路径生成，公共 component factory 永远为非 authority。
- Medium：fresh-hook 检查只覆盖 optimizer 实例 hooks，未拒绝 PyTorch global optimizer pre/post hooks；全局 hook 可在真实 AdamW step 前改变 optimizer/parameter groups。

已启动新的 `fix_ibr1_smoke_model_2b3` agent，要求补跨臂全部 module 与 parameter/buffer storage 区间不重叠、私有 live production provenance、全局 optimizer hook fail-closed 及对应 PoC 回归；修复完成后还必须再启动全新 reviewer，阻断项未清零前不允许实验。

同时新增原生 Windows authority CLI 小批次：

- `ibr1_experiment/cli.py` SHA-256 `c2d429c56a94a449249ba5805c85605c6e1cf311d3e0295ad1f9457942aa2788`
- `tests/ibr1/test_ibr1_cli.py` SHA-256 `b9d263ca2138e3571187264f4d0d2ffcdf0f03a36225b74d46ad5a5e5700d3c5`
- 模块 import 保持 stdlib-only，不提前加载 `torch`、IBR1 model、authority 或 CAL orchestrator；命令在首次导入 PyTorch 前固定 `CUBLAS_WORKSPACE_CONFIG=:4096:8`，再配置 frozen CUDA deterministic runtime。
- 当前命令覆盖 `preflight`、`build-bootstrap`、`run-cal-pair`、`build-final`、`verify-assembly`；正式 CAL pair 只能调用固定 parent-owned live subprocess orchestrator，所有输出继续固定 `formal_training_authorized=false`。
- 本地定向验证：`5 passed in 0.16s`；Ruff `All checks passed!`；py_compile 通过。使用 `E:\anaconda\envs\pytorch\python.exe` 与全新工作区 `--basetemp`。

CLI 独立 reviewer 尚未启动：当时四个 agent 席位均被两个返工、一个 smoke reviewer 和主线程占用；已列为下一可用席位任务。在 reviewer 验收前不得把该 CLI 当作 authority 放行证据。

## 2B smoke_model 第三轮复审与新 High（2026-07-20 19:19 +08:00）

源码返工后的第三轮独立复审验证了跨臂 module/parameter/buffer storage 区间、partial view alias、global/instance optimizer hooks、AdamW groups/device 等均已封闭；但仍发现一个 High，因而本轮仍为 BLOCK：

- `IBR1SmokePlan` 仍是可变 dataclass，`_production_provenance` 可在 component factory 返回后直接赋值为手造对象；`production_context`/`authority_eligible` 只检查对象类型和 module key，随后 `identity_receipt()` 会报告 `authority_eligible=true`，无需 live receipt/data SHA 重验。

复审复现：`BEFORE False/False -> AFTER True/True -> RECEIPT_AUTHORITY True`。这不是实验结果；未运行真实 CAL/EVAL/backward/optimizer/smoke，未访问 internal test。已再次派发修复任务，要求 component plan 与 production capability 不可后置升级，并在 authority 身份/执行入口重新验证完整 live provenance；修复后必须由新的独立 reviewer 复核。

第三轮复审其余验证：定向 smoke `25 passed`、相关 `47 passed`、Ruff 全绿；当前源码 SHA 仍为 `smoke_model.py 4dc6cbad8f150ec3881b78ef9008eb77ac43b97f1b283708f1fda395beb37916`、测试 `e34223b41b3ddd179b4a1177d0d8867ea76d52584a48360326e4d9d2dc1d3907`，待修复后更新。
## IBR1 2B smoke_model 第三轮 High 修复（2026-07-20 19:33 +08:00）

本节仅记录代码修复与本地回归，不是实验结果。真实 IBR1 CAL、EVAL、backward、optimizer update 与 authoritative smoke 均未运行，internal test 仍为 `sealed/opened=false`。

上一轮 reviewer 发现 component plan 可通过后置写入 provenance 升级为 authority。本轮已将 component 与 production plan 的 capability 分离：component 使用 slots 基类，永久 `production_context=false`、`authority_eligible=false`，且不携带 provenance 字段；私有 production 子类型通过外部 `WeakKeyDictionary` 注册 capability。production 的 identity、authority、arms、rows、G6 与执行入口都会重新核验 live final receipt 的路径、整文件 SHA、payload SHA、analysis class，以及 smoke data 的对象身份、support 顺序、reset set 与 token ledger provenance。

新增反例覆盖普通赋值、`object.__setattr__`、`dataclasses.replace`、copy/deepcopy、`__class__` 变更和直接构造 provenance，均不能把 component plan 升级为 authority；`close()` 仍可在不依赖 authority 校验的情况下幂等清理 optimizer hooks。跨臂 module/parameter/buffer storage 隔离、partial-view alias、global/instance optimizer hook 与 AdamW 合同回归保持通过。

- `ibr1_experiment/smoke_model.py` SHA-256：`fc01754083fbbf6543d651144b79c504b2f89215e927bb5c8168b58435ff94de`
- `tests/ibr1/test_ibr1_smoke_model.py` SHA-256：`27fd7f069501897cc26c29aae38bfefb03984415f030436fcb7897aa0eeedb77`
- 定向测试：`27 passed`
- 相关回归：`61 passed`
- non-authority IBR1：`127 passed, 54 deselected`
- Ruff、py_compile：通过

按团队流程已启动全新只读 reviewer `review_smoke_model_2b4`；在其 High/Medium 清零前，本修复不作为 authoritative smoke 放行依据。

## IBR1 CAL/authority 独立复审再次 BLOCK（2026-07-20 19:45 +08:00）

本节仍只记录代码审查，不是实验结果。reviewer 使用指定的 `E:\anaconda\envs\pytorch\python.exe`、Torch `2.6.0+cu124`、CUDA `12.4` 与 RTX 4060 Laptop，在全新工作区 basetemp 下得到定向 `56 passed in 122.84s`，Ruff 通过；未运行真实 CAL/EVAL/smoke，未访问 internal test。但复审仍报告 High `2`、Medium `1`、Low `0`，因此真实 CAL 保持禁止启动。

- High：lambda freeze 落盘后只剩普通 JSON；文件验证可重算字段，却无法重新证明历史上确有父进程观察到两个真实 `Popen` worker。当前 final assembly 仍可仅凭持久化 freeze 文件继续推进，未继承同一父进程内的 single-use live capability。
- High：生产 runner/factory/class 的身份检查仍依赖可修改的 `__module__`/`__qualname__` 字符串；需要改为真实对象、代码对象与绑定模块文件的身份校验。
- Medium：正式入口尚未硬锁 `E:\anaconda\envs\pytorch\python.exe`、Torch `2.6.0+cu124`、CUDA `12.4` 与 `cuda:0`，另一个自洽 CUDA Python 环境仍可能生成整条链。

审查时源码 SHA：

- `ibr1_experiment/authority.py`：`d5b325c3040e9a887c2bc57ad60527b545d05b98223724fd9a170c3c1cbb9a34`
- `ibr1_experiment/calibration.py`：`16926f262327613254f44addedfc9d3f3c56be91d2e2aa086aa59b1a60c38444`
- `ibr1_experiment/cal_pair.py`：`cb8917e9cf065245a466b301bf311cddd48255afa81c601d56e72682d6b87460`
- `ibr1_experiment/cal_worker.py`：`e6c8dfab5a79e7a5ef989baadcc2816348b43ff5cc9ab9521e54244add346aad`

已启动 `fix_cal_live_chain_2b5` 返工：production callable 改为真实 identity 校验；正式 runtime 固定到指定解释器/Torch/CUDA；file-only `build-final` 永久拒绝，final authority 只能在父进程实际启动固定双 worker 后，由同一调用内 single-use capability 生成。返工完成后仍需新的独立 reviewer 清零阻断。

## IBR1 smoke/gates 新一轮复审阻断（2026-07-20 19:53 +08:00）

本节仍不是实验结果。重新发起的 smoke reviewer 使用指定 CUDA Python 完成目标 `27 passed`、全 `tests/ibr1 181 passed in 297.63s` 与 Ruff 全绿，但仍报告 High `1`、Medium `0`、Low `0`：production plan 注册完成后，`_arms`、`paired_arms`、`g6` 等执行字段仍可被重新赋值；当前 live provenance 校验只覆盖 receipt/data，未把所有已注册执行对象的 identity 一并锁定。实测改写 `_arms` 后 `authority_eligible` 仍为 true，且 `close()` 会因执行映射损坏而异常。本轮 SHA 为 `smoke_model.py fc01754083fbbf6543d651144b79c504b2f89215e927bb5c8168b58435ff94de`、测试 `27fd7f069501897cc26c29aae38bfefb03984415f030436fcb7897aa0eeedb77`。

gates reviewer 的完整最终消息再次被平台误判中断，但其在中断前已完成定向 `55 passed` 与 Ruff，并明确定位三项一致性缺口：

- I2 仍信任记录携带的 `prebound_violation_mask`/`prebound_overshoot_fy`，没有逐元素从 `additive_prebound_fy` 重新计算并核对。
- result seal 虽核对 diagnostics 文件的 bytes/SHA/count/format，但没有从原始 training/eval JSONL 重算 summary 并与 I2/I3 gate receipt 交叉绑定。
- combined receipt 的验证未严格核对 `status`/`decision` 与 `passed`/`mechanism_pass` 的一致性。

另按此前审查线索，I1 正式入口也将收紧为必须同时提供并读取 project root、final assembly receipt path 与两个实际 update-0 sidecar path；纯内存 document/binding 不再作为 authority 用法。

已分别启动 `fix_smoke_plan_fields_2b6` 与 `fix_gates_consistency_2b6`。两批修复完成后都必须再交给全新 reviewer；在 High/Medium 清零前继续禁止真实 CAL、EVAL 与 smoke。

## IBR1 Windows CLI CUDA 初始化顺序修复（2026-07-20 20:20 +08:00）

本节是 Windows/CUDA 工程预检留痕，不是 CAL、训练、EVAL 或 smoke 实验结果。现场发现 `ibr1_experiment.cli._configure_runtime()` 先调用 `require_official_torch_cuda()`；真实 PyTorch 会在 `cuda.is_available()`/`set_device(0)` 时初始化 CUDA，随后 `configure_cuda_reproducibility()` 必然以“确定性策略设置过晚”拒绝所有 `preflight/build-bootstrap/run-cal-pair` 命令。

已把顺序修正为：预先冻结 `CUBLAS_WORKSPACE_CONFIG` → 导入 PyTorch → 配置完整确定性 CUDA 策略 → 校验并选择官方 `cuda:0`。新增回归测试强制证明第一次 CUDA availability/device query 发生在 reproducibility 配置之后。

验证结果：

- `tests/ibr1/test_ibr1_cli.py`：`9 passed`
- Ruff（`ibr1_experiment/cli.py`、`tests/ibr1/test_ibr1_cli.py`）：通过
- 真实命令 `E:\anaconda\envs\pytorch\python.exe -m ibr1_experiment.cli preflight --project-root E:\AAAI\track_car`：成功
- runtime：Python `E:\anaconda\envs\pytorch\python.exe`、PyTorch `2.6.0+cu124`、CUDA runtime `12.4`、`cuda:0`、RTX 4060 Laptop
- authority chain payload SHA：`1e54ec0a02cb0da58eef8abaa131f4de0fe734ab8b3b8f05a648945b8b372dff`
- F2 negative seal SHA：`b85585c8232f65c75d5958abb7d51d7624db4031c9adec86ee570d0a5b7378e7`
- `formal_training_authorized=false`；internal test 保持 `sealed/opened=false`

新独立 reviewer `review_cli_cuda_order_2b7` 结论：High `0`、Medium `0`，审查通过；复跑定向测试 `9 passed`、Ruff 通过、真实 Windows/CUDA preflight 成功。未运行 CAL、训练、F2 或 internal test。

## IBR1 最终放行复核与首次 Windows/CUDA 启动留痕（2026-07-20 23:51–23:55 +08:00）

本节记录真实 IBR1 lifecycle 的首次启动与工程失败 burn，不是科学 gate 结果。F2 仍为既有 `FAIL / STOP`，禁止重跑；IBR1 的 `formal_training_authorized=false`，internal test 仍为 `sealed/opened=false`。

启动前最终代码与审查状态：

- HEAD：`b10b0de Make IBR1 result seal the final write`；前序固定合同提交：`4b6c6b5 Freeze IBR1 seal reset and eval contracts`。
- 官方解释器完整回归：`tests/ibr1` 为 `221 passed in 351.24s`；Ruff 为 `All checks passed!`；`compileall -q ibr1_experiment tests/ibr1` 通过。
- 新独立 reviewer `review_seal_atomicity_b10`：High `0`、Medium `0`、Low `0`；确认 `plan.close()` exactly once、summary/close 失败不写科学 seal、成功时 seal 为最后一次 authoritative write。
- 新独立 reviewer `review_eval_constants_4b6`：High `0`、Medium `0`、Low `0`；确认 SMK-TRAIN static resets 精确为 `12`、两臂分别固定，六个 EVAL-FIX phase static resets 精确为 `28`，每份 receipt 精确绑定 `DEFAULT_CONFIG.to_dict()` 与 `EVAL_MODE_CONTRACT`。
- 启动就绪审计：`GO`。官方环境为 Python `E:\anaconda\envs\pytorch\python.exe`、PyTorch `2.6.0+cu124`、CUDA runtime `12.4`、`cuda:0`、RTX 4060 Laptop；authority-chain payload SHA 为 `1e54ec0a02cb0da58eef8abaa131f4de0fe734ab8b3b8f05a648945b8b372dff`。
- 启动前 GPU baseline：used `1747 MiB`、free `6211 MiB`、total `8188 MiB`。未结束千问、Wallpaper Engine 或任何用户进程。

唯一 fresh bootstrap：

- 路径：`experiments/windows_cuda_ibr1/assembly_receipt_cuda_bootstrap_v1.json`
- 文件 SHA-256：`21bcaa4b0f003d72118d72b20320bf3a95058c29dca0a6ef6a2e0a62fd0ab7ca`
- receipt payload SHA-256：`860fa74ed798514713d23468b15379a8f6777853b027b4019800cc38094ab8b5`
- phase：`bootstrap`
- `lambda_freeze_binding=null`
- `formal_training_authorized=false`
- `verify-assembly --phase bootstrap` 成功。

真实启动：

- 启动时间：`2026-07-20T23:53:23+08:00`
- 父进程 PID：`21376`
- 入口：`python -m ibr1_experiment.cli run-smoke`
- CAL：`experiments/windows_cuda_ibr1/cal_audit_cuda_v1`
- freeze：`experiments/windows_cuda_ibr1/cuda_lambda_adoption_freeze_v1.json`
- final：`experiments/windows_cuda_ibr1/assembly_receipt_cuda_final_v1.json`
- smoke：`experiments/windows_cuda_ibr1/smoke_cuda_v1`
- stdout/stderr 与启动元数据：`experiments/windows_cuda_ibr1/runtime/`

工程失败 burn v1：

- 失败时间：`2026-07-20 23:55:32 +08:00`；阶段：`live_cal_pair_freeze_final`。
- lifecycle 已写入 `smoke_cuda_v1/candidate_lock.json`，随后 main CAL worker 在读取任何 CAL row、构建 optimizer 或执行 update 前退出。
- 根错误：`F2AssemblyContractError: assembly receipt does not freeze a token ledger anchor; CAL and smoke are forbidden until build-assembly-receipt freezes one`。
- IBR1 bootstrap 的 `asset_binding.observation` 已记录 `token_ledger_sha256/token_ledger_file_count`，但复用的 F2 subordinate CAL kernel 从 `asset_binding` 顶层读取这两个冻结 anchor；该跨 family 兼容绑定缺口未被原测试/preflight/verify-assembly 捕获。
- 未生成 CAL receipt、lambda freeze、final assembly、update-0 checkpoint、optimizer update、EVAL receipt、I1–I6、combined receipt 或科学 result seal。
- `smoke_cuda_v1/engineering_failure.json` 明确记录：`valid_scientific_result=false`、`result_seal_written=false`、`rerun_requires_fresh_output_directory=true`；failure receipt payload SHA-256 为 `eb7d2b31e41bab74e215c908dcf45fdcd898dc68dd2b986e17c49cabb5ab1a8f`。
- v1 bootstrap、candidate lock、engineering failure、stderr、PID 与 launch metadata 全部保留，不删除、不覆盖、不在原目录重试。
- 修复必须仅补齐 token-ledger authority bridge，不改变 IBR1 family、seed、128 updates、lambda、gates、internal-test policy 或 formal authorization；修复经新独立 reviewer 验收后，使用全新 v2 bootstrap/CAL/freeze/final/smoke 路径重新启动。

## IBR1 token-ledger bridge 修复与 v2 CAL/freeze 工程 burn（2026-07-21 00:09–01:43 +08:00）

v1 根因修复与代码审查：

- `44963ab9c441b8570a4e3605e70ba62ddb77ca82 Freeze IBR1 token ledger anchor for F2 CAL`：在 IBR1 self-hashed `asset_binding` 顶层冻结与 `observation` 完全一致的 `token_ledger_sha256/token_ledger_file_count`；lifecycle 在创建 smoke/candidate lock 前首验，cal-pair 在 CAL 目录/Popen 前复验。
- 官方 Python 完整回归：`tests/ibr1` 为 `227 passed in 329.43s`；Ruff 与 compileall 通过。
- 独立 reviewer `review_preoutput_order_449`：High `0`、Medium `0`、Low `0`；定向 `9 passed`、authority+lifecycle `59 passed`，旧 v1 bootstrap 在任何新输出/Popen 前拒绝。
- 独立 reviewer `review_token_bridge_449`：High `0`、Medium `0`、Low `1`。唯一 Low 为自定义 observer 的 `36946.0` 可被数值相等与 `int(...)` 归一化接受，不影响默认生产 observer，但不满足严格 JSON 类型合同。
- `bfff5e279012f1efb89d0c96a1977309a6c18ecb Enforce exact IBR1 token ledger count type`：要求 outer/observation count 均为原生 `int` 且精确等于 `36946`，拒绝 float/bool/string，移除 `int(...)` 隐式归一化。
- 该 Low 修复的官方验证：新增定向 `6 passed`、authority `50 passed`、Ruff/compileall 通过；全新 reviewer `review_ledger_type_bfff` 结论 High `0`、Medium `0`、Low `0`，最窄回归 `7 passed`。

v2 bootstrap 与启动：

- bootstrap：`experiments/windows_cuda_ibr1/assembly_receipt_cuda_bootstrap_v2.json`
- 文件 SHA-256：`596f31a2494b3ff64738d703065147944289c5a603ec5fef45325a3fb7411850`
- payload SHA-256：`217ca8888442817fde2aac14a86122c5c9ec4acf8cd0ff47a086fa067fa70138`
- source-binding payload SHA-256：`ef0f967811a1cbaec414e7536a38f60314c889954c92d7587100dacdc00903c8`
- test-binding payload SHA-256：`e1b3b75d91512d174763a3d1f91a1d5542f4bc51b4deeb4c1702350a4018a028`
- `freeze=null`、`formal_training_authorized=false`；outer/inner ledger SHA 均为 `a963170846db0ef2890b45f056ec9884a713ce4223bea260722ccc25c62448d6`，count 均为严格整数 `36946`。
- `verify-assembly --phase bootstrap` 成功；启动前 GPU used `1712 MiB` / free `6246 MiB`。
- 真实 v2 父进程：PID `19768`，启动时间 `2026-07-21T00:41:39+08:00`；启动元数据与日志位于 `experiments/windows_cuda_ibr1/runtime/smoke_cuda_v2.*`。

v2 真实 CAL 结果：

- main worker PID `21716`，reproduction worker PID `34784`，均使用官方 `E:\anaconda\envs\pytorch\python.exe`、CUDA `12.4`、`cuda:0`，exit code 均为 `0`。
- 两次 CAL 均完成 512-row zero-update audit；运行时峰值显存约 `5733 MiB / 8188 MiB`，无 OOM。
- raw F2 receipt SHA（main/reproduction 相同）：`2c16c674abd4f5da166bb4527ec8b9a8dc4b230b4fd12bd5dbc49ab1d436d32d`
- numeric evidence SHA（相同）：`01410950aac7a0d40bdc366890030d9df81f4cc73c37c821fe0273d77aef790b`
- core SHA（相同）：`3088ef2992c7d65a89fbee5dd69712fbaba22b82359e4b3610664f9727ddc8d5`
- envelope SHA（相同）：`ba7649d0ec19e7c093595c9f1f1c0e3e1579cafb1733c6fb484f0374dbaa5aa6`
- execution witness 因 role/PID/challenge 不同而整文件不同：main `65b6567ee370b5e7c523e8c7cd6eef66e260b35e015caebd369039ea739729f4`，reproduction `e44c5872f654f8ac14d190e78bfc8aab4c4193f17c6545a71f38f5b3374db0fd`；其 callback/scientific transcript 由 parent 验证一致。
- parent 冻结证据确认：`raw_f2_byte_identical=true`、`numeric_evidence_byte_identical=true`、`core_byte_identical=true`、`envelope_byte_identical=true`、`distinct_processes_verified=true`。

v2 lambda freeze：

- 路径：`experiments/windows_cuda_ibr1/cuda_lambda_adoption_freeze_v2.json`
- 文件 SHA-256：`df5b67c815b974732c141758a66b887218bc2f5efcd0d564b99ba4d3545815db`
- payload SHA-256：`99ba388518e80a05b9489509669baa88a39b890e68413600dad526171ba0fa62`
- adoption：`FROZEN_EXACT_INHERITANCE`
- lambda：`L_cot=0.0195`、`L_future=0.34`、`L_verify=0.5`
- `authority_eligible=true`、`formal_training_authorized=false`、internal test `sealed/opened=false`。

v2 工程性能停止：

- CAL/freeze 后，authority verifier 对同一 bootstrap 的嵌套调用重复全量重建 36,946-file token ledger。只读调用图审计确认：`verify_cal_pair` 展开为约 `27` 次 bootstrap live verify；freeze 后的 freeze/final/final-verify 还会继续嵌套放大。
- freeze 写入时父进程累计读盘约 `198 GiB`；后续仍以约 `7–13 GiB/45s` 增长。静态上完整链可能达到多 TiB 与十几小时以上，属于有限但病态的工程性能缺陷，不是 CUDA/GPU 训练慢，也不是科学 gate FAIL。
- 在 `final assembly` 尚未写入、optimizer 尚未构造、optimizer updates=`0`、checkpoint/EVAL/I1–I6/combined/scientific seal 均为 `0` 时，主线程仅按精确 PID+命令行终止自有实验父进程 `19768`；未结束任何用户程序。终止后 IBR1/run-smoke/CAL worker 残留进程为 `0`。
- v2 外部工程停止记录：`experiments/windows_cuda_ibr1/smoke_cuda_v2/engineering_stop_external_monitor.json`；`valid_scientific_result=false`、`scientific_result_seal_written=false`、`rerun_requires_fresh_output_directory=true`。
- v2 bootstrap、CAL pair、freeze、candidate lock、runtime logs/metadata 与外部停止记录全部保留；不得覆盖或在 v2 原目录重试。
- 下一步仅优化同父进程内的不可序列化 verified-evidence/capability 复用，消除重复 ledger 重哈希；不得减少 worker 边界、source/data drift、TOCTOU、file-only/replay 或 optimizer 前的必要复核。修复经新 agent 实现及全新 reviewer 验收后，必须使用 fresh v3 bootstrap/CAL/freeze/final/smoke 路径。

## IBR1 authority 重哈希有界化、ABA 修复与 v3 放行（2026-07-21 02:17–02:42 +08:00）

本节是 v2 工程停止后的代码修复、独立审查与启动前留痕；在本节记录时尚未启动 v3 CAL、训练、EVAL 或科学 gate。F2 仍为既有 `FAIL / STOP`，negative seal SHA 仍为 `b85585c8232f65c75d5958abb7d51d7624db4031c9adec86ee570d0a5b7378e7`；禁止重跑 F2、换 seed、调 gate 或启动 formal 9-run。IBR1 继续固定 family `IBR1 / interval_bounded_residual_v1`、seed `0`、`128` updates、CAL `512` rows、lambda `0.0195 / 0.34 / 0.5`、`formal_training_authorized=false`，internal test 保持 `sealed/opened=false`。

性能修复提交：

- `e211e004cc6fa6c4b19c7ca73c41ec56de95b468 Bound IBR1 authority rehash cost`：加入同父进程 `ContextVar` verification session、`WeakKeyDictionary` registry-backed opaque snapshot、root/PID/thread/task/lifetime 绑定、custom observer cache bypass、canonical receipt bytes 重读、fresh 边界与同 session final issuance bootstrap 复用。
- 父进程完整 authority ledger rebuild 固定为 `7` 次：smoke pre-output、pre-worker、post-worker、post-forensic/pre-freeze、final postwrite、pre-execution、pre-seal；`load_ibr1_smoke_data` 另做 `1` 次 train ledger traversal，因此父进程总目标与硬上限均为 `8`，不再是 v2 的约 `1305` 次/约 `8.7 TiB` 病态放大。
- worker 内仍各自在 CAL entry/exit 做 live bootstrap 验证；plan 普通属性访问与嵌套 forensic verifier 只重读冻结 receipt bytes，不增加 heavy ledger rebuild。

首轮全新安全 reviewer 对原始 `e211e00` 报告 High `0`、Medium `1`：cached receipt 校验失败后旧 snapshot 未立即烧毁，receipt ABA 恢复后可能重用旧 snapshot。主线程未启动实验，随后修复公共 verifier；第二名全新 reviewer 又发现 final issuance 的 `_session_bootstrap_snapshot()` 是同类旁路，因此继续保持 BLOCK。

最终修复提交：

- `7265cdd Burn failed IBR1 authority snapshots`：公共 cached verifier 与私有 final-issuance bootstrap snapshot 两个生产调用点均在任何 `BaseException` 后精确删除同路径 snapshot 并重抛；只有完整 live rebuild、receipt 最终重读和一致性比较全部成功后才重新写入 snapshot。
- 新增 ABA 回归证明：公共 cache 漂移/恢复后 ledger 次数 `1 -> 2`；final issuance cache 漂移/恢复后 ledger 次数 `2 -> 3`。正常 `100` 次 nested/cache 验证仍不增加 ledger，custom observer、跨 root/thread/task、过期 session 与 file-only authority 仍 fail closed。

独立审查结论：

- `review_e211_security_fresh`：原始 `e211e00` High `0`、Medium `1`（上述 ABA，已修）；Low `2` 为同线程 copied-context hardening 与 capability consume 使用 same-session cached final snapshot。后者会重读 exact final bytes，紧接 fresh final-postwrite 且无外部 callback，之后仍有 data-loader ledger、pre-execution fresh 与 pre-seal fresh，因此不构成当前合同阻断。
- `design_review_authority_memo`：High `0`、Medium `0`；独立 instrumentation 确认 cal-pair 父进程 fresh 次数 `3`，完整 parent authority `7` 次加 data-loader ledger `1` 次，总计 `8`；pre-execution fresh 位于 data/weights/optimizer 构造后、任何 checkpoint/EVAL/optimizer 执行前，pre-seal fresh 保留。
- `review_aba_all_consumers_fresh`：对最终工作树 High `0`、Medium `0`、Low `0`；确认 `_verify_cached_assembly_snapshot` 仅有两个生产调用点且两处均异常烧毁，focused `4 passed`、Ruff 与 diff-check 通过，结论为可以启动 v3。

验证结果：

- `e211e00` 完整 `tests/ibr1`：`234 passed in 318.65s`；Ruff、py_compile、diff-check 通过。
- `7265cdd` 后完整 authority：`54 passed in 115.20s`。
- lifecycle：`13 passed`；ABA focused：`3 passed`；最终 reviewer focused：`4 passed`。
- Ruff、py_compile、diff-check 全部通过。
- 当前关键 SHA-256：`authority.py 41bceb0d3f84dc53f35475477111ffc9fe7fa9f65c5cb7530df21d72534696c0`；`cal_pair.py 2b7b0298bded4c20da06b90ea60e07a297515613167929e24bdbdcd3b518cb7f`；`lifecycle.py 30e955a751bc51fdcb80bcb34a0eac2e47f55b95466da8b3d07c3b27c335eedc`；`test_ibr1_authority.py 37bb6c9b2e2ccbedd42a8b2c572b55d3f5ed50f094d4efc42613e58e13cb9cc8`。

启动前官方 preflight（`2026-07-21 02:24 +08:00`）成功：Python `E:\anaconda\envs\pytorch\python.exe`、PyTorch `2.6.0+cu124`、CUDA runtime `12.4`、`cuda:0`、NVIDIA GeForce RTX 4060 Laptop GPU；authority-chain payload SHA `1e54ec0a02cb0da58eef8abaa131f4de0fe734ab8b3b8f05a648945b8b372dff`；`formal_training_authorized=false`；internal test `sealed/opened=false`。GPU baseline：total `8188 MiB`、used `1711 MiB`、free `6247 MiB`、utilization `15%`、temperature `54 C`。未结束千问、Wallpaper Engine 或任何用户进程。

放行结论：代码与独立复审均已清零 High/Medium。下一步只能生成 fresh v3 bootstrap，并由同一个 detached `run-smoke` 父进程连续执行 CAL main/reproduction → lambda freeze → final assembly → paired 128-update smoke → EVAL/I1–I6/combined → PASS seal 或 negative `SEAL_STOP`；禁止单独先运行 `run-cal-pair`，禁止复用/覆盖 v1/v2 目录。

### v3 fresh bootstrap 与启动前冻结（2026-07-21 02:43–02:45 +08:00）

- 已逐项确认以下 fresh 目标均不存在：`assembly_receipt_cuda_bootstrap_v3.json`、`cal_audit_cuda_v3/`、`cuda_lambda_adoption_freeze_v3.json`、`assembly_receipt_cuda_final_v3.json`、`smoke_cuda_v3/` 及 `runtime/smoke_cuda_v3.*`；同时确认无残留 `run-smoke` 或 `cal_worker` 进程。
- 使用官方 Python 执行 `ibr1_experiment.cli build-bootstrap`，未单独运行 `run-cal-pair`。
- bootstrap 路径：`experiments/windows_cuda_ibr1/assembly_receipt_cuda_bootstrap_v3.json`
- 文件 SHA-256：`faf90c4f2bea9b1c0bfd8c9331fd189a8a3782889a7df411824eccb8747ab1c6`
- receipt payload SHA-256：`1e631aa9d4b7e003e6efa1e409e408d08d67d3e03891f18e87c51baba4ae1da6`
- source-binding payload SHA-256：`969e216cb309d679e1656fefc4016ff61c11a729f6da57cd30371e063f871591`
- test-binding payload SHA-256：`164ec186e4104872172c50ae7031789d0ae1d3d5c0655ff93c883043f7182f17`
- phase：`bootstrap`；`lambda_freeze_binding=null`；`formal_training_authorized=false`；internal test `sealed/opened=false`。
- outer 与 observation token ledger 均为 SHA `a963170846db0ef2890b45f056ec9884a713ce4223bea260722ccc25c62448d6`、原生整数 count `36946`。
- `verify-assembly --phase bootstrap` 已完成第二次 live 核验并成功；自此禁止修改、增删任何被该 receipt 绑定的 Python、测试、模型、训练数据、cache/ledger、PRIMARY/F2 seal 文件。Markdown 实验记录与新 runtime 元数据不属于 source binding，可继续追加。

### v3 detached 真实启动（2026-07-21 02:46:29 +08:00）

- detached 父进程 PID：`27248`
- 官方入口：`E:\anaconda\envs\pytorch\python.exe -m ibr1_experiment.cli run-smoke`
- bootstrap：`experiments/windows_cuda_ibr1/assembly_receipt_cuda_bootstrap_v3.json`
- CAL pair：`experiments/windows_cuda_ibr1/cal_audit_cuda_v3/`
- lambda freeze：`experiments/windows_cuda_ibr1/cuda_lambda_adoption_freeze_v3.json`
- final assembly：`experiments/windows_cuda_ibr1/assembly_receipt_cuda_final_v3.json`
- smoke：`experiments/windows_cuda_ibr1/smoke_cuda_v3/`
- runtime 留痕：`experiments/windows_cuda_ibr1/runtime/smoke_cuda_v3.pid`、`smoke_cuda_v3.launch.json`、`smoke_cuda_v3.stdout.log`、`smoke_cuda_v3.stderr.log`
- 启动后观测：PID/命令行/官方解释器均匹配；stdout/stderr 初始为 `0` bytes；父进程 CPU `23.52 s`、working set 约 `739 MiB`、private memory 约 `1002 MiB`，正在 authority/ledger 预验阶段。
- 启动时 GPU：total `8188 MiB`、used `1715 MiB`、free `6243 MiB`、utilization `14%`、temperature `54 C`。未终止或修改任何用户进程。
- 本次必须继续监控到科学 PASS seal、negative `SEAL_STOP`，或新的工程 failure burn；不得在 v3 原路径覆盖重试。

运行阶段更新：smoke pre-output fresh 通过后，`candidate_lock.json` 于 `02:47:20` 落盘；父进程累计读盘约 `13.68 GB` 后完成 pre-worker fresh。main CAL worker PID `21444` 于 `02:48:11` 由父 PID `27248` 启动，role/path/challenge 命令行均匹配冻结合同。worker-entry ledger 复核后 GPU 显存升至 `5254 MiB`（free `2704 MiB`），worker private memory 约 `6.16 GiB`，说明真实 CUDA 模型与 512-row zero-update CAL 已开始；stderr 仍为 `0` bytes，无 OOM/CPU fallback/工程错误。已启动全新只读 agent `monitor_v3_cal_main` 独立监控 main worker 阶段转换。

### v3 CAL/freeze/final 成功与 update-0 checkpoint 工程 burn（2026-07-21 02:55–03:17 +08:00）

v3 两个真实 worker 均成功完成官方 CUDA 512-row zero-update CAL：main PID `21444`，reproduction PID `27176`，共同父 PID `27248`，官方 Python/Torch/CUDA/device 均匹配，exit code 均为 `0`。两次 CAL 的 raw F2、numeric、core、envelope 四类科学文件逐字节相同：

- raw F2 SHA：`c733d63e4b890501bf37907ef127e49f2a7c7a4c6492109f02a46740abfb3434`
- numeric SHA：`78a90ffc3a8b2a40142968d124f41dd1e6cd83f8c475f658572b29d5fa618e58`；payload `19f403e8134340fce672c93ffd41855abfaf2152341f8151e29cf22a77e455dc`
- core SHA：`40b8e5d9d878f3104b78cf4123c82b1d557cbe509e8bc66b0b65d57e7731074d`；payload `57e9e49354d3a6812da30b43ccb229ee543a4b07b6dd079920f5f2f2ba07fe68`
- envelope SHA：`2dea9ad632aa9761cd415a27c994652aca9a083b1135423919f66807039f0654`；payload `d5766a3144e95d3ca78eb922f1f3bb9c1b034e7da4c74c642894fbdf36a0c1ac`
- witness 按 role/PID/import token 设计为不同文件：main SHA `42b49c5b232cb8c962bdb9493983d7ec6f443200a85902c14340838a546d46e4`，reproduction SHA `5abfed8c0fd6608f7706ba8ce410c6d724830c1a4dae42638d6edcb84329efc8`；bootstrap/runner/production binding 一致。

CAL 证据确认：`512` callbacks、optimizer updates `0`、zero-init failures `0`、range violations `0`、reconstruction error `0`、prev-free/reachability failures `0`；seed `0`、device `cuda:0`、lambda proposal 严格为 `0.0195 / 0.34 / 0.5`；internal test `sealed/opened=false`。

freeze/final authority 均成功且重新 live verify 通过：

- lambda adoption freeze SHA：`45b57946b714020f201ca2c5e056a23305fa3e3046ebe1f858ba32ec5ffa8539`；payload `f3e4fe...36bf`；10 个 CAL artifact SHA 均与现场文件匹配，distinct/live parent authority 为真，file-only forensic authority 为假。
- final assembly SHA：`a73b834f95a2cb16f74f82a5cc728fe5221fba61d628cd4831b19893ea707873`；payload `d2af1a7a169f002575671935ea56696a127526d62604d2b8d39617b2fdcab2a8`；bootstrap→final 的 source/test/support/assets/authority/F2-negative 六组 binding 完全一致，ledger 仍为 `36946 / a9631708...8d6`，`formal_training_authorized=false`。

随后进入 `execute_authoritative_smoke`，两臂 update-0 checkpoint 与 sidecar 均成功写入，但 lifecycle 在任何 EVAL 或 optimizer update 前 fail closed：

- CTRL checkpoint SHA：`2040fbb353ea6c05a2e388e744ad1328fa2d1082ece07c49dd6be4d37daf6250`
- SELF checkpoint SHA：`836a7925b4b821d54851eaf218f5c03e607773e326a685f686e576c4a6f9f353`
- 两个 sidecar 记录的 `checkpoint_tensor_sha256` 相同：`99e2c9d208aabcaa5eaadba70822f7f209c3d15b1f3fe4560e79b27132fc89ab`
- 工程错误：`IBR1LifecycleError: update-0 checkpoint tensor identities differ from sealed init`
- engineering failure SHA：`2a440d19740f2fdcab794585d02490a1d7627ce9739347fedbd2f83d0f6ef3ac`；payload `ba4be98057eac5439f9d9e85fd81139bd423faa9145fc0c41391d40654c362e5`；stage `execute_authoritative_smoke`、`valid_scientific_result=false`、`result_seal_written=false`、`rerun_requires_fresh_output_directory=true`。

精确边界：未创建 optimizer update、count receipt、update-128 checkpoint、任何 EVAL receipt、diagnostics、I1–I6、combined receipt、smoke summary、PASS seal 或 negative scientific seal。因此 v3 是工程 burn，不是科学 PASS/FAIL，不授权续跑、覆盖或同目录重试。

两路独立诊断与真实 checkpoint probe 证明模型没有漂移：每臂 payload 均为 adapter `424` + model `25`，合计 `449` tensors、原始 tensor bytes `1,393,656,816`；CTRL/SELF 的 key/dtype/shape/逐 tensor SHA 差异数均为 `0`；`.pt` 文件 SHA 与 sidecar 一致。对同一 payload：旧 IBR1 section-domain hash 为 `99e2c9...89ab`，按 frozen F2 `adapter.* / model.*` full-state domain 重算为 `74f838e314dd6f3b208dbed23e0e7a92dcdde09bb7d79b0ce8c1147d9b251e54`，精确等于 sealed init。根因不是 Windows、CUDA、torch.save、alias 或权重变化，而是 `checkpoint.py` 使用 `ibr1-checkpoint-tensors-v1` domain，而 lifecycle/gates 与 runner 使用 `f2-checkpoint-init-v1` domain，错误地直接比较两种 framing。

### checkpoint hash-domain 修复与 v4 放行（2026-07-21 17:00–17:12 +08:00）

- 提交：`ab98eac Align IBR1 checkpoint tensor hash domain`。
- `checkpoint_tensor_sha256()` 保留非空 mapping、合法 key、tensor 类型、strided/non-quantized/non-meta、finite 校验；随后恢复真实 `full_state_dict()` 的 `adapter.* / model.*` 命名，并委托 frozen F2 `checkpoint_init_sha256()`。未删除 lifecycle 或 gate 检查，未改变 checkpoint schema、authority 字段、seed、lambda、gate 或训练逻辑。
- 新增回归：F2 full-state domain 等价；任一 tensor 漂移改变 SHA；CPU/CUDA 结果一致；真实双臂 update-0 save 后两份 tensor SHA 均精确等于 sealed init；save/load 与 sidecar/file 漂移拒绝保持通过。
- 验证：focused `3 passed`；checkpoint `20 passed`；lifecycle `13 passed`；gates `28 passed`；完整 `tests/ibr1` 为 `238 passed in 358.12s`；Ruff、compileall、py_compile、diff-check 全部通过。
- 全新 reviewer `review_checkpoint_hash_fix_fresh`：High `0`、Medium `0`、Low `0`，结论 `GO`；独立复跑 checkpoint `20 passed`、lifecycle+I1 `14 passed`，CUDA 用例未跳过。
- 当前 SHA：`ibr1_experiment/checkpoint.py 45b34cb74983d760dce5bfa2163d8f8b4cb40fa32dd8ded2c759f3ed4075de46`；`tests/ibr1/test_ibr1_checkpoint.py 9c527003b4b6c7b9c0e4a60a67ffb08503666e9bf8b2a76c7ed5d44637cf608c`。

v3 的 bootstrap/CAL/freeze/final/checkpoints/failure/runtime 日志全部保留为历史 burn；当前 source/test SHA 已因 `ab98eac` 改变，任何 v3 authority 都不能复用。下一步只能重新跑官方 preflight，生成 fresh v4 bootstrap，并由一个新的 detached `run-smoke` 父进程使用全套 `*_v4` 路径重新执行。

### v4 fresh preflight 与 bootstrap 冻结（2026-07-21 17:14–17:20 +08:00）

- 启动前逐项确认 `assembly_receipt_cuda_bootstrap_v4.json`、`cal_audit_cuda_v4/`、`cuda_lambda_adoption_freeze_v4.json`、`assembly_receipt_cuda_final_v4.json`、`smoke_cuda_v4/` 与 `runtime/smoke_cuda_v4.*` 均不存在；没有残留的 Python `run-smoke`、`run-cal-pair` 或 `cal_worker` 进程。既有 dirty/untracked 工作区保持原样，未清理、未覆盖历史实验。
- 官方 preflight 使用 `E:\anaconda\envs\pytorch\python.exe` 成功完成：PyTorch `2.6.0+cu124`、CUDA runtime `12.4`、`cuda:0`、NVIDIA GeForce RTX 4060 Laptop GPU；deterministic contract 为 `windows_cuda_deterministic_v1`。
- authority-chain payload SHA：`1e54ec0a02cb0da58eef8abaa131f4de0fe734ab8b3b8f05a648945b8b372dff`；F2 negative seal SHA：`b85585c8232f65c75d5958abb7d51d7624db4031c9adec86ee570d0a5b7378e7`。
- 科学约束保持不变：`formal_training_authorized=false`；internal test `sealed/opened=false`；没有重跑 F2、换 seed、调 gate 或启动 formal 9-run。
- fresh bootstrap 路径：`experiments/windows_cuda_ibr1/assembly_receipt_cuda_bootstrap_v4.json`。
- bootstrap 文件 SHA-256：`e39e7503f3c13b6515f46b0efab97be424f19f5656aee2a7252a6e890b132df4`；receipt payload SHA-256：`c6e0339b60aea35d3195be413122155a4ed6555bc01587b99251306dc026b7ed`。
- `verify-assembly --phase bootstrap` 已完成 live 核验并退出 `0`。自此禁止修改、增加或删除 receipt 绑定的 Python、测试、模型、训练数据、cache/ledger、PRIMARY/F2 seal；实验 Markdown 与新的 runtime 元数据可继续追加。
- 独立只读启动审计 `audit_v4_launch_readiness` 结论为 `GO`：启动前路径 fresh、无旧实验进程、官方环境与封存约束全部匹配。bootstrap 独立 reviewer `review_bootstrap_v4_fresh` 已启动，等待最终 High/Medium/Low 结论。

### v4 detached 真实启动（2026-07-21 17:22:49 +08:00）

- 唯一 detached 父进程 PID：`36984`；解释器：`E:\anaconda\envs\pytorch\python.exe`；入口：`python -m ibr1_experiment.cli run-smoke`。
- 本次没有单独调用 `run-cal-pair`。同一父进程按冻结生命周期连续执行 CAL main/reproduction → lambda freeze → final assembly → paired 128-update smoke → EVAL/I1–I6/combined → PASS seal、negative `SEAL_STOP` 或工程 failure burn。
- bootstrap：`experiments/windows_cuda_ibr1/assembly_receipt_cuda_bootstrap_v4.json`。
- CAL：`experiments/windows_cuda_ibr1/cal_audit_cuda_v4/`；freeze：`experiments/windows_cuda_ibr1/cuda_lambda_adoption_freeze_v4.json`；final：`experiments/windows_cuda_ibr1/assembly_receipt_cuda_final_v4.json`；smoke：`experiments/windows_cuda_ibr1/smoke_cuda_v4/`。
- runtime 留痕：`experiments/windows_cuda_ibr1/runtime/smoke_cuda_v4.pid`、`smoke_cuda_v4.launch.json`、`smoke_cuda_v4.stdout.log`、`smoke_cuda_v4.stderr.log`。
- 启动后核对实际命令行与所有 v4 路径完全匹配，父进程存活且响应；初始 CPU `16.14 s`、working set 约 `737 MiB`、private memory 约 `1001 MiB`，stdout/stderr 均为 `0` bytes，尚处于 authority/ledger 预验阶段。
- 启动附近 GPU：driver `560.94`、total `8188 MiB`、used `1712 MiB`、free `6246 MiB`、utilization `9%`、temperature `54 C`。未终止或修改千问、Wallpaper Engine 或任何其他用户进程。

阶段更新：独立 bootstrap reviewer `review_bootstrap_v4_fresh` 已完成，结论 `GO`，High `0`、Medium `0`、Low `0`。其独立 `verify-assembly --phase bootstrap` 退出 `0`；44 个 source binding、15 个 test binding、16 个 authority/evidence 文件共 75 个文件 SHA 全部匹配，canonical JSON、自哈希、F2 negative seal、CUDA deterministic contract 与所有授权字段均通过。

`candidate_lock.json` 于 `2026-07-21 17:23:47 +08:00` 落盘。main CAL worker PID `24052` 于 `17:24:40` 由唯一父 PID `36984` 启动；解释器为官方 Python，命令行 role=`main`、bootstrap=`assembly_receipt_cuda_bootstrap_v4.json`、output=`cal_audit_cuda_v4/main`、parent PID=`36984`，challenge 已现场生成并绑定。stdout/stderr 仍为 `0` bytes，无 failure/seal。已启动全新只读 agent `monitor_v4_cal_main` 监控 512-row zero-update CAL，并启动 `review_v4_launch_identity` 独立复核父进程、launch metadata 与 candidate lock。

独立启动身份 reviewer `review_v4_launch_identity` 结论 `GO`，High `0`、Medium `0`、Low `0`：唯一 run-smoke PID、唯一 main worker、五条 v4 argv、PID/launch/log 路径、bootstrap file/payload SHA 全部一致；75 个 authority 绑定重新计算为 `0 mismatch`。candidate lock 文件 SHA 为 `9b8732ad...793b`，payload SHA 为 `bba385889a820ebc3119f56fda5edd5660923f1ad1bb61de55b536e33974a56e`，cap `1`、index `0`、seed `0`、SA-Hstar，retry/换 seed/调 lambda/decode/gate 均为 false。OS 中已退出的 detached launcher 原 PPID 仅作信息性记录，不影响存活父进程及其 worker authority。

### v4 main CAL 完成（2026-07-21 17:24:40–17:37:05 +08:00）

- main worker PID `24052` 正常退出；父进程接受 return code、canonical stdout、恰好五文件与 witness identity 后，于 `17:37:06` 启动 distinct reproduction PID `39728`。
- raw F2 SHA：`2af14425f9d93f54efb536eb9ace2b141a513ed2af4a12cf1bc25dc2b88889af`。
- numeric SHA：`534a9d658f210f9c30b1e07adaca7359002dd8169823c8c564d167f0494caab5`。
- core SHA：`d01d388b9825b0cb2738a90b43bd6a0617266fbd76e4ea2703bad07770838177`。
- envelope SHA：`84467a0a944bcbb65e6b417e96f9f7c07635af4b4076846f6b58595ddaa87f2a`。
- execution witness SHA：`f364e183199e158b8671cff80b7413e014386dab3504f60891199454e96c4780`。
- 512 callback records 的 position `0..511` 与逐条哈希链重算通过；records SHA `0c317077...04c1`、final SHA `8a60454e...a2f7`，与已成功的 v3 CAL 科学 transcript 相同。worker 记录 duration `742.8159074 s`。
- rows=`512`、optimizer updates=`0`、seed=`0`、device=`cuda:0`、PyTorch=`2.6.0+cu124`、CUDA=`12.4`；全部 failure/violation 为 `0`，envelope `passed=true`；lambda=`0.0195 / 0.34 / 0.5`，checkpoint init=`74f838e3...251e54`，internal test `sealed/opened=false`，`formal_training_authorized=false`。
- 全新只读 monitor `monitor_v4_cal_main` 结论 `GO`，High `0`、Medium `0`、Low `0`。另一个独立 reviewer `review_v4_main_cal_artifacts` 仍在复核同一五文件。reproduction 阶段已改由新 agent `monitor_v4_cal_reproduction` 独立监控。

第二名独立 main reviewer `review_v4_main_cal_artifacts` 也给出 `GO`，High `0`、Medium `0`、Low `0`；其调用仓库 `verify_cal_execution_witness` 做了约 `493.5 s` 的 fresh 全链验证，覆盖 live bootstrap/source/assets、五文件及 512-row transcript，退出即刻与完整验证后五文件 SHA 均未漂移。raw canonical payload SHA 为 `285cb7da...955d`。

### v4 reproduction CAL 完成与 pair 等同性（2026-07-21 17:37:06–17:50:22 +08:00）

- reproduction worker PID `39728`，父 PID `36984`，role=`reproduction`，使用与 main 相同的 parent challenge、bootstrap 与官方 CUDA 环境，并作为 distinct process 正常退出。
- reproduction raw/numeric/core/envelope SHA 依次为 `2af14425...889af`、`534a9d65...caab5`、`d01d388b...38177`、`84467a0a...87f2a`，与 main 四文件逐项完全相同。
- reproduction witness SHA 为 `dad3560bea8cd89f3256dd53506082a4102cfd13192abd0c26144ad9978b8678`，与 main witness `f364e183...c4780` 不同；两者分别绑定 role/PID `main/24052` 与 `reproduction/39728`，共同绑定 parent PID `36984` 与同一 challenge，符合执行见证合同。
- reproduction stdout/stderr 无错误，父进程仍存活并已进入 post-worker live authority/forensic verification；记录时 freeze/final 尚未写出。
- 已启动全新只读 agent `review_v4_cal_pair` 复核两次 CAL 与后续 freeze；`monitor_v4_cal_reproduction` 继续完成 reproduction 阶段独立结论。

reproduction monitor 最终结论 `GO`，High `0`、Medium `0`、Low `0`；其连续观测真实 CUDA 显存约 `5730 MiB`、GPU utilization `8–41%`，确认 main/reproduction 都是 512 callbacks、0 updates，四类科学产物逐字节一致，witness 剔除 role/PID/token/clock 动态字段后其余内容完全一致。CAL-pair reviewer 也给出 `GO / 0-0-0`。

### v4 lambda freeze、final 与 update-0 实机修复验证（2026-07-21 17:59–18:05 +08:00）

- lambda freeze 于 `17:59:24` 落盘，文件 SHA `6f95045c9efd508c16737b98631255ca0f6deeb3fe41612f540e06b204ba34cc`，payload SHA `d476045f78a3bc629dd0c80274e7b7441e03b691690bbef5078fb6a720440fe9`；live CAL pair、十文件与 frozen lambda `0.0195 / 0.34 / 0.5` 均被接纳。
- final assembly 于 `17:59:37` 落盘，文件 SHA `c97e59d32c7bda3bab18efaa47f0681eba432525ce6437e9b54ef6e00a041911`，payload SHA `adec0aee7eb16a0eac19860a54a36aaa76a1717e775f836e7dd26306350c71a6`；phase=`final`、source/test/assets/authority/F2-negative 与 freeze binding 保持闭合，`formal_training_authorized=false`、internal test `sealed/opened=false`。
- CTRL update-0 checkpoint 于 `18:05:10–18:05:12` 写入：文件 SHA `9b6a223562eba213aed5a3874347f780f21136b68704d7211bc3f06cdfdc67c4`，sidecar SHA `28d436b2125d19e35bd428e90961b2c89f6509cc692a1068035955c9b0083232`。
- SELF update-0 checkpoint 于 `18:05:23–18:05:25` 写入：文件 SHA `eea4401f587af3bea90da2eb4f5d7b0e7066f99932a826324c2e5d7feb27fbba`，sidecar SHA `d01f14b33d68d538e75ce80f0a91af42bb5250b36d489b23df0fa24576a4dabc`。
- 两臂均为 adapter `424` + model `25` = `449` tensors，tensor bytes `1,393,656,816`，checkpoint bytes `1,393,828,784`；独立反序列化重算的 `checkpoint_tensor_sha256` 均精确为 sealed init `74f838e314dd6f3b208dbed23e0e7a92dcdde09bb7d79b0ce8c1147d9b251e54`。旧 v3 section-domain `99e2c9...` 未出现，证明 `ab98eac` 修复在真实 Windows/CUDA v4 中生效。
- 全新 update-0 reviewer 结论：阶段 `GO`，High `0`、Medium `0`、Low `0`；checkpoint/sidecar/final authority、arm identity、shape/dtype/schema 全部通过，且不包含 optimizer/RNG state。

### v4 首个训练更新前 diagnostics 工程 burn（2026-07-21 18:06–18:07 +08:00）

- update-0 后仅生成 SELF 的两份冻结 EVAL：`eval_phase_u0_self_logged.json` SHA `79d630ee50c2a76a19ac7c8b0188463178dc2706e180f65c2e460d9deca14204`，`eval_phase_u0_self_self.json` SHA `f4fa6b7dcf63dba32e6ece4add30c6b891f9216917118e151da0b2d03dda9b65`；均为 512 rows、static resets `28/28`。
- 首个 S-CTRL `u_pre=0` 完成两行 gradient accumulation 后，runner 在调用 `hooks.g6_update(update_event)` 时进入 `GradientDiagnosticsCollector.observe_contributions()`，以 `IBR1DiagnosticsContractError: weighted per-aux vectors do not reconstruct aggregate g_aux` fail closed。
- runner 顺序明确为 `g6_update` 在 `state.callbacks.optimizer_step(update_event)` 之前；因此 CTRL 与 SELF 的实际 optimizer updates 均为 `0`。没有 update-128 checkpoint、count receipt、训练/EVAL 完整集合、diagnostics artifact、I1–I6、combined receipt、summary 或科学 seal。
- engineering failure 文件 SHA：`1bfb696cd51ea5eb2ade21b7b6f4326c8b5e0270eca4316b118b762bdeee121f`；payload SHA：`08d8fb21898c4c546d6c58866985d21725b9daef54116583d7dc0b252e489f39`；`stage=execute_authoritative_smoke`、`valid_scientific_result=false`、`result_seal_written=false`、`rerun_requires_fresh_output_directory=true`。
- 父 PID `36984` 已退出，无 `run-smoke/cal_worker/run-cal-pair` 残留。本 v4 是新的工程 burn，不是科学 PASS/FAIL；全部 bootstrap/CAL/freeze/final/checkpoint/EVAL/failure/runtime 目录永久保留，不覆盖、不续跑、不复用。
- 已启动全新根因 agent `diagnose_v4_gaux_reconstruction`；下一步只允许修复 diagnostics 的真实合同/数值问题，经新 reviewer 验收后使用 fresh v5 全路径重启。

### v4 `g_aux` 根因确认与 exact joint VJP 修复（2026-07-21 18:07–19:31 +08:00）

- 对 v4 final assembly、真实 SMK-TRAIN 首两条样本和生产 `base.proj`（`2,626,560` 维）做只读 CUDA probe 后确认：三次独立 per-aux VJP 相加与真实 aggregate VJP 的 update-0 mean 存在 L2 `0.00536495`、max `0.00069213` 的数值差异。反向图穿过冻结 BF16 Qwen 共享主干，原 `16 * float32_eps` 重构阈值不成立；这不是 Mac/Windows 路径或 CUDA 不可用问题。
- 拒绝采用经验性 FP32/BF16 容差，因为它可能同时放过较小辅助项被遗漏。生产 probe 中分别遗漏 `L_cot / L_future / L_verify` 时，联合梯度 L2 差异约为 `0.204 / 0.205 / 0.222`。
- 修复改为固定顺序 `L_cot → L_future → L_verify` 的 exact joint VJP：逐 row 先要求 aggregate scalar 与 ordered joint scalar 在同 device/dtype 下 `torch.equal`，再对三个输出执行一次 all-one cotangent tuple VJP，并要求该 joint VJP 与真实 aggregate VJP 逐 row `torch.equal`。三次独立 VJP 仅保留为每个辅助项的 norm/cosine/projection 描述，不再承担权威重构证明；I3 reconstruction discrepancy/bound 严格固定为 `0.0`。
- 新增 fail-closed 覆盖：component/aggregate detach、`detach().requires_grad_()` 独立叶子、probe 不可达、缺失/额外 key、shape/device/dtype 漂移、精确抵消、同值漏梯度、深共享图 BF16 漂移，以及跨连续 update accumulator 清理。检查使用 `torch.autograd.grad`，不累积参数 `.grad`，不增加训练 `backward`、optimizer step 或 RNG 消耗。
- 定向合并回归：`46 passed in 341.17s`。完整 `tests/ibr1`：`246 passed in 503.14s`。Ruff、compileall、py_compile、`git diff --check` 全部通过。
- 独立 reviewer `review_gaux_joint_fix_fresh`：`GO`，High `0`、Medium `0`、Low `0`；另一路 `review_joint_vjp_math_fresh`：High `0`、Medium `0`、Low `1`，唯一 Low 为注释把 `torch.equal` 表述成 bit-identical 且未准确说明 `autograd.grad`，已在提交前改为“同 device/dtype 下 exact equal、且不累积 `.grad`”，不影响算法与放行。
- 修复提交：`dfa820ad6699fc9ba463260e5ee3bcc4ac81f54d Use exact joint VJP for IBR1 auxiliary gradients`。
- 最终文件 SHA-256：`ibr1_experiment/diagnostics.py b6b71a3047cd9b7251f4188c07e32f4ffeac874c8f248ec30b28bb223a803386`；`ibr1_experiment/gates.py 8ccfe56b352798eed38552371b8df81443280e98fb3b75a42e097a92d2ee9342`；`tests/ibr1/test_ibr1_diagnostics.py f83de582fa13115021c418d6b8ca671d59daaf6637055c672978ea770d59e5e1`；`tests/ibr1/test_ibr1_gates.py b5e2f8d274d1f1f45cc9f6d8bc64f7b5721e27ae30667fb2a8f577cb0044ebbf`。
- v4 全部产物继续作为工程 burn 永久保留。任何 v4 bootstrap/CAL/freeze/final/checkpoint/EVAL/failure authority 均不得复用；下一步只允许 fresh v5 preflight、bootstrap、verify 和唯一 detached `run-smoke` 父进程。

终审更新：注释修订后的全新 reviewer `review_joint_vjp_final_text` 给出 High `0`、Medium `0`、Low `0`，确认 `torch.equal` 与 `autograd.grad` 表述准确，四文件 joint VJP 最终 diff 未出现新问题，Ruff 与 diff-check 通过；该 reviewer 未修改工作区。

### v5 fresh preflight、bootstrap 与 detached 启动（2026-07-21 19:32–19:37 +08:00）

- 启动前确认 bootstrap、CAL、freeze、final、smoke 及四个 runtime v5 目标共九条路径全部不存在；无残留 `run-smoke`、`run-cal-pair`、`cal_worker` 或 pytest Python 进程。既有 dirty/untracked 工作区与 v1–v4 历史产物原样保留。
- 官方 preflight 退出 `0`：解释器 `E:\anaconda\envs\pytorch\python.exe`，PyTorch `2.6.0+cu124`，CUDA runtime `12.4`，`cuda:0` NVIDIA GeForce RTX 4060 Laptop GPU，deterministic contract `windows_cuda_deterministic_v1`。authority-chain payload `1e54ec0a02cb0da58eef8abaa131f4de0fe734ab8b3b8f05a648945b8b372dff`，F2 negative seal `b85585c8232f65c75d5958abb7d51d7624db4031c9adec86ee570d0a5b7378e7`；`formal_training_authorized=false`，internal test `sealed/opened=false`。
- fresh bootstrap：`experiments/windows_cuda_ibr1/assembly_receipt_cuda_bootstrap_v5.json`；文件 SHA-256 `75807e5e1216bd0184f3000b082725a4b5de8e60918532c3679169052c2c4966`，receipt payload SHA-256 `add4888644d888e85a931bd1898c88df05391798637127518946f50192908d0b`。主线程 `verify-assembly --phase bootstrap` live 核验退出 `0`。
- 独立只读启动审计 `prepare_v5_launch_audit` 结论 `GO`，High `0`、Medium `0`、Low `0`；再次确认 v5 路径 fresh、官方 CUDA 环境、固定 seed `0`、CAL `512` rows、每臂 `128` updates、lambda `0.0195 / 0.34 / 0.5`，并确认禁止单独运行 `run-cal-pair`、禁止重跑 F2、换 seed、调 gate 或 formal 9-run。
- 唯一 detached 父进程 PID `44016` 于 `2026-07-21T19:36:34+08:00` 启动；实际命令为官方 Python 的 `-m ibr1_experiment.cli run-smoke`，五个 argv 路径严格指向 bootstrap/cal/freeze/final/smoke v5。runtime 留痕为 `runtime/smoke_cuda_v5.pid`、`.launch.json`、`.stdout.log`、`.stderr.log`。
- 启动后进程存活且响应，CPU `10.67 s`、working set `740.04 MiB`、private memory `1003.49 MiB`；stdout/stderr 均为 `0` bytes。附近 GPU baseline：driver `560.94`、total `8188 MiB`、used `1731 MiB`、free `6227 MiB`、utilization `9%`、temperature `53 C`。未终止或修改任何用户进程。
- 已启动全新只读 agent `review_bootstrap_v5_fresh` 与 `review_v5_launch_identity`；后续每到 main CAL、reproduction/freeze/final、update-0、训练/EVAL/seal 阶段继续使用新的 reviewer/monitor 留痕。

阶段更新：`candidate_lock.json` 于 `2026-07-21 19:37:49 +08:00` 落盘，文件 SHA-256 `9b8732adf2723fb9ad2a7edf2086e2cbca34abe273095ddbb57040900816793b`，payload SHA-256 `bba385889a820ebc3119f56fda5edd5660923f1ad1bb61de55b536e33974a56e`；candidate cap `1`、index `0`、seed `0`、package `SA-Hstar`，retry/换 seed/调 lambda/decode/gate 均为 false。

main CAL worker PID `41640` 于 `2026-07-21 19:39:24 +08:00` 由唯一父 PID `44016` 启动；解释器为官方 Python，role=`main`，bootstrap/output 均为 v5，parent challenge `152b568ab9b72857c6f26c5804d7dd538abb8cd957cffbc3f63d76577fc5a6fe`。此阶段预期执行 512-row、zero-update 真实 CUDA CAL；stdout/stderr 仍为空，无 failure/seal。

独立启动身份初审 `review_v5_launch_identity`：`PASS`，High `0`、Medium `0`、Low `0`。其现场重算 bootstrap/candidate lock 文件与 payload SHA 均一致，确认唯一存活 run-smoke 父 PID、detached launcher 已退出、完整 v5 argv、main worker parent/role/challenge 以及 seed/candidate/retry/调参禁令全部匹配。

bootstrap 独立终审 `review_bootstrap_v5_fresh`：`GO`，High `0`、Medium `0`、Low `0`。其 fresh `verify-assembly` 用时 `101.1 s` 并退出 `0`；44 个源码、15 个测试、17 个 authority/F2 引用、训练 ledger `36946 / a9631708...248d6` 以及冻结模型/cache/negative seal 全部 live 重算无漂移。Windows/CUDA deterministic 细项与全部授权字段均通过。

### v5 main CAL 完成并启动 reproduction（2026-07-21 19:39:24–19:55:02 +08:00）

- main worker PID `41640` 正常退出，五文件齐全，runtime stdout/stderr 均为 `0` bytes。raw/numeric/core/envelope/witness 文件 SHA-256 依次为 `c38110094a7e2ebd9cad7877d6fe945739457bc4046d1a7818d07465bb3d141e`、`f6da208cd126b7162d00534cc67ea588ff850c51de87d6fa291abbbd9dd92921`、`4d8c2f08b566eb580c1620cd56e6d01e03764d115172b9d44846eb6313fb9863`、`23c27347570be89edfcd947e4cd5b7e9106760571e4631f8d1ace72814545d52`、`5d537fdbd17afc0a20e418202d8e34010f962ddfcb9cfaa27d05474985d5535e`。
- 512 callback positions `0..511`、records SHA `0c3170770f207f3eda61f4304603e179027ed357e04fbb156ae88773bd9e04c1`、final SHA `8a60454e2566ff40200e3a8611764f43be2e8ef46ef8f05fb0ebc8fe5f22a2f`；optimizer updates `0`，seed `0`，device `cuda:0`，checkpoint init `74f838e3...251e54`，lambda proposal `0.0195 / 0.34 / 0.5`，zero-init/reachability/reconstruction failures 均为 `0`，envelope `passed=true`。
- 与 v4 main 的逐顶层字段比较显示科学 transcript、CAL 数值、lambda 和全部 checks 完全一致；v5 文件 SHA 变化只来自预期的 bootstrap/source/binding/self-hash 与 worker identity/clock 字段，因为本轮 authority 已绑定提交 `dfa820a`。
- 独立 main monitor 终审：`GO`，High `0`、Medium `0`、Low `0`；其重新机械构造 canonical 五文件并完整重算 512 行链、bootstrap execution binding、CUDA deterministic policy，全部通过。
- 同一父 PID `44016` 于 `19:55:02` 自动启动 distinct reproduction PID `15164`；role=`reproduction`，使用同一 challenge `152b568a...5a6fe`、同一 v5 bootstrap 与官方 CUDA 环境。已将 reproduction 交给新的只读阶段审查任务。

### v5 reproduction CAL、lambda freeze 与 final（2026-07-21 19:55:02–20:12:44 +08:00）

- reproduction PID `15164` 正常退出，五文件 canonical/self-hash 与 512-row 链完整通过；worker 为官方 PyTorch `2.6.0+cu124` / CUDA `12.4` / `cuda:0`，optimizer updates `0`，lambda `0.0195 / 0.34 / 0.5`。
- main/reproduction 的 raw、numeric、core、envelope 四文件逐字节相同，SHA 依次为 `c3811009...d141e`、`f6da208c...d92921`、`4d8c2f08...b9863`、`23c27347...45d52`。reproduction witness SHA `c51716b4dbefe8bc7afc6aa3278e33b6dddfd599ce3b6d092f2e60e3765e2f43`，与 main witness `5d537fdb...535e` 不同；差异只限预期的 role、PID、process/module token、clock、child PID 与 self-hash，parent PID/challenge 和所有科学 binding 保持一致。
- reproduction 独立终审：`GO`，High `0`、Medium `0`、Low `0`；确认两 worker exit `0`、四项 byte-identical、身份 token distinct、没有 retry/seed/lambda/gate 漂移。
- lambda freeze 于 `20:12:30` 写出：文件 SHA `5b0f3b8c534ef46c5572986c1120a4c662cb97049fef9eec3729d4f3791bd12b`，payload SHA `9848c0b137c9ad8cfa367076f5d090915e929effeb76ae32c41c12dd40d69cb2`，status=`FROZEN_EXACT_INHERITANCE`，proposal 与 frozen values 均严格为 `0.0195 / 0.34 / 0.5`。
- final assembly 于 `20:12:44` 写出：文件 SHA `0812eee83a2a3a30e03a79d762cd6c148417fdf106405fcc40ab4faba6672161`，payload SHA `1b3636473ec9ab014df0b539d687bef26e913742686faa88e994723e844be307`；phase=`final`，绑定 fresh v5 bootstrap/CAL/freeze，`formal_training_authorized=false`，internal test `sealed/opened=false`。
- 父 PID `44016` 持续健康，进入 final live verification 与 paired smoke continuation；runtime stderr 无错误。

CAL-pair/freeze/final 第二路独立终审也为 `PASS`，High `0`、Medium `0`、Low `0`：逐行重算 512-row callback/execution 链、reset `30`、records/final SHA、梯度中位数与机械 lambda 派生均通过；bootstrap→CAL→freeze→final 的文件/payload/live attestation binding 全部一致。

### v5 update-0 与首个真实 optimizer step（2026-07-21 20:18:47–20:23:38 +08:00）

- CTRL update-0 checkpoint/sidecar SHA 分别为 `4fcb53e33c666d3f63fbb6e5b0c032f0e23252577c178cfc3b305d2b69b4f12e` / `7dd9f0842e847d98519ee631ac52fe2332c3566886510421247247eac8111a24`；SELF 分别为 `0fb241892b119fea31595f88045a06cb5d7b842b06175123bcbab814540dde20` / `6d337803a9d719cb38d957a84e57d3ddcab67a86b16e068262678f68bbb4c4d1`。
- 两份约 `1,393,828,784` bytes checkpoint 均已独立 stream-SHA、CPU `weights_only` 加载并按 F2 full-state domain 重算；adapter `424` + model `25`，tensor SHA 均精确为 sealed init `74f838e314dd6f3b208dbed23e0e7a92dcdde09bb7d79b0ce8c1147d9b251e54`。arm/u_pre/schema/source/snapshot policy/final v5 authority binding 全部一致。
- 两份 u0 SELF EVAL 于 `20:19:56` / `20:20:54` 完成。runtime stderr 的 `588` bytes 仅为两条 TensorFlow oneDNN INFO，无 traceback 或工程错误。
- v4 在第二份 EVAL 后约 `7 s` 于首个 `g6_update(u_pre=0)` fail closed；v5 在相同边界后继续运行超过 `160 s`，GPU 约 `6.18 GiB / 37%`，父 PID `44016` CPU 持续增长，无 `engineering_failure`。结合冻结 runner 顺序，首个 row-level scalar/joint VJP、row-level aggregate/joint VJP 与 update-level exact check 已全部通过，随后真实 AdamW pre/step/post hook 完成，optimizer step count 已从 `0 → 1`；I3 首记录 aggregate discrepancy 与 rounding bound 均为 `0.0`。
- 独立 update-0/首更新 reviewer：`GO`，High `0`、Medium `0`、Low `0`。这证明提交 `dfa820a` 已在真实 Windows/CUDA production smoke 中修复 v4 的工程阻断；持久化完整 diagnostics/count/update-128 仍需等待两臂训练完成。
- reviewer 的一次只读长校验在工具壳超时后留下 PID `41508` 孤儿；主线程核对其官方 Python `-` 命令、已退出 parent 和无写操作后，仅停止该任务自有 verifier，未触碰父实验或任何用户进程，也未把该未取得 stdout 的校验作为通过证据。

### v5 128-update paired training 与 update-128 checkpoints（2026-07-21 20:20:54–20:27:55 +08:00）

- `count_receipt.json` 文件 SHA `90acf1e1d4c19c51e03ae154121caf57e39a1e48df1df09571b2ab51522f4937`，状态 `PASS / GO`。两臂各 `256` rows、`128` optimizer steps，grad accumulation=`2`；CTRL/SELF G7 各 `128`、G9 各 `256`，CTRL-only G6=`128/0`，static resets 各 `12`、nonfinite resets `0`，feature/aux/head/track-loss/backward/controller、泄漏与 overwrite 计数全部精确匹配冻结合同。
- CTRL update128 checkpoint 文件/sidecar SHA 为 `1bc402e1b1dc9d478dce566a6dd3dc84ffe83b5a7dfb7198b46d3f7ba4bd6898` / `a7470349097e13672c84c069609f34adde0bf05166b00219c78c14f1933bd5fb`，独立 tensor SHA `48d9bd2264df58efa21a5a25712e86e6e8d82f0c096bd7fda4b12e0fb5b9150e`。
- SELF update128 checkpoint 文件/sidecar SHA 为 `f2a3a12aa1f95d10416dafd3a6dbb23e84727418685808cd3061fe25f54de127` / `fa41190b37a187c9c53d45cc3770cfe8283b8fc82d94ce916a90238cea5a71e5`，独立 tensor SHA `f859ce9de0c15d2c1df772e0f6e40bf82a96ae1149906d93093c6fa70a3fa13c`。
- 两臂均为 adapter `424` + model `25` 精确 schema，arm/u_pre/source/snapshot/final authority binding 全部通过。相对共同 update0 sealed init，两臂各有 `109 / 449` tensors 精确变化，且 CTRL/SELF update128 tensor SHA 不同，证明真实训练已发生且臂身份没有混淆。
- 独立 count/update128 reviewer：`GO`，High `0`、Medium `0`、Low `0`；其对两份 checkpoint 做了 CPU `weights_only` 读取和实际 tensor hash/diff，而非只信任 sidecar。

### v5 u128 EVAL、diagnostics/gates 与最终 seal 工程 burn（2026-07-21 20:28:43–20:36:56 +08:00）

- 四份 u128 EVAL 依冻结顺序完成，文件 SHA 依次为 CTRL logged `5c99847a7d9fdab1f9ffbb89d6e8cc33dbf1f5ce852616e8b79cf1b587992a53`、CTRL self `6c2bc4ef623dfd9f602b38169e95b71fae2a13b42a2ad13010f70366252ea272`、SELF logged `de1b4c51553e540b94e39ef00d7146b12a0f44bddcfc0e0cdfccaa82eeda9598`、SELF self `f92eb04972fef720172489831b5ee45da21cd64706965198d1f86c85fd3a7214`；每相 512 finite rows、static resets `28/28`。eval guard SHA `4b2fed40b483daf707ad87e802a7710b7fb74bb2e3f0657c93973121aa99f93f`，六相/3072 calls/order/predictor identity 均通过独立审查。
- diagnostics 六文件全部写出；gradient records `128`（u_pre `0..127`），optimizer records `256`，所有 `per_aux_aggregate_discrepancy_norm` 与 `per_aux_aggregate_rounding_bound_norm` 的唯一值均严格为 `0.0`，证明 exact joint VJP 修复贯穿完整 128 updates。
- I1/I4/I6 为 PASS；I2/I3/I5 为 FAIL，combined gate `status=FAIL / decision=SEAL_STOP`。主要未达科学阈值：I2 forward-bound violation rate CTRL `0.86376953125`、SELF `0.8759765625`（阈值 `<0.05`）；I3 inherited G6 ratio `6.308432626497229 > 1.5`、cosine `0.16026081013931662 < 0.6`、positive projection `73 < 108`；I5 inherited G8 gap contraction与 turn/other strata improvement 未通过。这些 gate 是有效工程输入，但最终科学负结果尚未封存。
- 在 `build_ibr1_negative_result_seal()` 阶段，父进程以 `IBR1GateContractError: EVAL guard binds a different final assembly` fail closed。engineering failure 文件 SHA `d24de0735ff541c469a1b5716fa98ab5493c502e966034cfa9fd27cfa02c5175`，payload SHA `eeec6e74bf79d3f6bd20da405d9d7614e7bd719d4416f1be1cb71f4742bcb863`；`valid_scientific_result=false`、`result_seal_written=false`、`rerun_requires_fresh_output_directory=true`。
- 精确根因不是 path/SHA/payload 漂移：EVAL guard 的 final assembly binding 只有 `{path, sha256, receipt_payload_sha256}`，三个值都与 final 完全相同；seal 的 `_artifact_binding()` 还包含 `analysis_class=ibr1_assembly_source_receipt`，严格 dict equality 因单个缺失字段失败。真实 eval_guard 单测冻结了三字段，而 seal 测试使用人工四字段 fixture，遗漏真实 guard→seal 接口集成。
- 父 PID `44016` 已退出，无实验 worker 残留；没有 authoritative smoke summary、PASS seal 或 negative scientific seal。v5 全部 bootstrap/CAL/freeze/final/checkpoint/EVAL/diagnostics/gates/failure/runtime 产物永久保留为工程 burn；不能修补现有 receipt、不能同目录重试，修复与复核后只能 fresh v6。

### v5 seal 第二阻断、双修复与 fresh v6 代码放行（2026-07-21 20:37–21:54 +08:00）

- 为定位第一个 EVAL binding 阻断，诊断过程只在临时副本中补齐标准四字段 binding 后重放 negative-seal builder；没有修改、覆盖或续跑任何 v5 authority/artifact。该精确重放立即暴露第二个独立阻断：`IBR1GateContractError: bound I3 receipt differs from diagnostics raw-data recomputation`。
- I3 完整文档差异仅为 inherited G6 的 cosine median：生产 receipt 为 `0.16026081013931662`，旧 diagnostics replay 为 `0.1602608101393307`，差值 `1.4072076837123859e-14`；由此 I3 self-hash 改变。I2 与 I5 已分别验证完整文档精确一致，未发现第三个 gate replay mismatch。
- 根因是生产 `G6Instrument` 对 live FP64 accumulator 直接计算 `torch.dot(sum_aux + sum_track, sum_track)`；旧 seal replay 只持久化 aux-track dot 与 norms，再用 `aux_track_dot + track_norm**2` 代数重建 total-track dot。两者数学等价，但 reduction/addition order 不同，不能保证 IEEE-754 binary64 位级相同。生产路径此前只做 close geometry cross-check，而 result seal 要求完整 gate receipt 精确相等，因此必须保存真实 live 值，禁止放宽 receipt equality、rounding、subset comparison 或修改 frozen F2 evaluator。
- EVAL binding 修复：`IBR1EvalOrderGuard` 在验证 final assembly 的 `analysis_class==ibr1_assembly_source_receipt` 后，finalize binding 固定输出 `{path, sha256, receipt_payload_sha256, analysis_class}`。PASS/negative seal 对完整四字段字典继续做精确相等，旧三字段 receipt 不兼容并保持 fail closed。
- I3 exact replay 修复：每条 gradient record 新增完整七字段 `exact_g6_update`，显式包含 `u_pre / aux_reachable / track_reachable / cosine_total_track / signed_projection / aux_track_ratio / per_aux_ratios`，包括 warmup 的显式 `null`。`IBR1G6Instrument` 先写 absolute geometry，再调用原 `super().emit_update(event)` 取得真实 `G6Update`，随后 once-only 绑定到同一 clock record，并原对象返回；`finalize()` 要求 128/128、外层与嵌套 clock 连续、schema/type/window/B* 规则全部精确成立。
- `evaluate_i3()` 现在把 supplied live update 与持久化 exact mapping 都规范为严格七字段，并用 canonical JSON bytes 精确比较；一 ULP、`+0.0/-0.0`、缺键、多键、错误 clock/type/window 均拒绝。原 absolute norm/dot、frozen lambda、reachability、Cauchy/total-norm identity、projection/ratio close cross-check 仍保留；`per_aux_aggregate_discrepancy_norm` 与 rounding bound 仍必须严格为 `0.0`。`_g6_updates_from_gradient_geometry()` 已完全删除代数重建，只读取持久化 exact mapping 后重新运行 frozen `evaluate_g6(block_mode='bstar')`。
- 固定 regression 使用 `sum_aux=[0,1]`、`sum_track=[1,1]`：live direct cosine `0.9486832980505138`，旧代数 reconstruction `0.9486832980505139`；live projection `2.1213203435596424`，旧 reconstruction `2.121320343559643`，两项均恰差一 ULP。120 个 gradient-window updates 使用该向量后，production I3 与 exact diagnostics replay 的完整 dict、payload SHA 和 canonical bytes 精确一致；旧公式会稳定产生不同 receipt。
- 真实 producer→consumer 集成回归不再同时使用 synthetic 两端：真实 `IBR1EvalOrderGuard` 执行 6×512 predictor calls 并 `finalize()`，同一 receipt 直接进入真实 PASS/negative builder，再执行真实 `freeze_ibr1_result_seal()` 与 `verify_ibr1_result_seal()`；PASS 与 negative 两路均覆盖。仅将上游昂贵 assembly verifier seam 指向同一 canonical 磁盘 final receipt，未 mock guard、seal builder 或 seal verifier。
- 首轮三个改动文件完整测试结果为 `74 passed, 1 failed`；唯一失败是旧 PoC 在篡改 absolute geometry 后仍期待后续 `bound I3 receipt differs`，而新实现更早在独立 signed-projection cross-check fail closed。测试随后改为同步更新 exact mapping、保持篡改 bundle 内部自洽，再确认最终 bound I3 receipt 仍被拒绝；该单项重跑 `1 passed`。这不是实现放行或科学结果变化。
- 最终官方环境完整 `tests/ibr1`：`270 passed in 823.81s`。Ruff 范围 `f2_experiment ibr1_experiment tests/f2 tests/ibr1` 为 `All checks passed!`；`compileall`、六个改动文件 `py_compile`、`git diff --check` 全部通过。
- 四路全新只读 reviewer 最终均为 GO，High `0`、Medium `0`、Low `0`：`review_real_guard_seal_fresh`、`review_exact_g6_fresh`、`review_combined_seal_contract`、`review_exact_g6_schema_final`。审查中发现并修复的唯一 Medium 是 supplied Mapping 曾允许省略 optional keys 后补 `None`；最终已改为 supplied/persisted mapping 都必须 exact 7-key set，并增加 missing/extra/signed-zero 回归。
- 代码与测试提交：`66d078a57fdc225b33f45c4020c1709d84d9031b Persist exact IBR1 G6 seal evidence`。该提交仅包含 `ibr1_experiment/diagnostics.py`、`gates.py`、`eval_guard.py` 与三个对应测试文件，未提交或清理工作区其他既有改动。
- v5 的 failure SHA `d24de073...c5175`、全部 authority/CAL/checkpoint/EVAL/diagnostics/gates/runtime 继续永久保留，`valid_scientific_result=false`、`result_seal_written=false` 不变；严禁回填 `analysis_class` 或 `exact_g6_update` 后补封存。下一次只能使用全新 v6 bootstrap/CAL/freeze/final/smoke/runtime 路径，由唯一 `ibr1_experiment.cli run-smoke` 父进程连续完成；不得单独运行 `run-cal-pair`，不得重跑 F2、换 seed、调 lambda/decode/gate 或启动 formal 9-run，internal test 继续 `sealed/opened=false`。
